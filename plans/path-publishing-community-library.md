# Plan: Path publishing & community library (V-model phased delivery)

> Companion to project memory `project_path_publishing_idea.md`. Approved 2026-05-20.

---

## 1. Context — why we are doing this

Three pre-existing constraints converge on the same answer:

- **Freemium unit economics**: per-free-user AI COGS must stay ≤ ~0.3 CHF target / ~1 CHF hard ceiling. The current free-tier allowance of 3 AI-generated paths/month at `checkUsageLimit('ai_study_plan')` is the dominant cost driver — a Pro path generation today routes through Anthropic Sonnet / Gemini for structure + theory + flashcards + quizzes per slot. Free users are not unit-positive at this allowance.
- **Paths-led growth**: paths are the front door. Marketing leans on them. Free users **must** experience a path within seconds of signup or the funnel breaks.
- **Single Pro tier**: one paid tier has to cover everything for free users — no PLUS subsidy lane. So either free users pay (no), free generation gets gated tighter (kills the funnel), or **free users get paths from somewhere that isn't a fresh AI call**.

The intersection is: **let users publish their paths**, **seed a curated library**, **let other users browse / clone / translate** — and **pull AI path generation out of the free tier entirely**, because the value prop ("I have a Duolingo-style path") survives without per-free-user AI burn. Translation costs are amortised across users with a `(path_id, language)` cache. Discovery doubles as a growth surface.

The side-effect is that the moderation pipeline forces us to finally build the **admin dashboard** that doesn't exist yet — admin APIs exist (`/api/admin/waitlist`, `/api/admin/stats`, `/api/admin/users`) but there is no UI binding them together.

---

## 2. Why V-model (not iterative "ship & fix")

Most of this project's existing plans use phased forward-execution with a final manual-test pass (e.g. `plans/personal-duolingo-rework-manual-tests.md`). V-model is stricter: **each build phase has its own paired verification gate, and the next phase does not start until the gate passes.**

This is the right shape for this work because:

- **Moderation surfaces are correctness-critical.** A wrongly-approved path is public; a wrongly-published wordlist bypass is a brand risk. Iterating live is not acceptable.
- **Cost gates are part of correctness here.** Each AI-using phase (L2, L3, translation, pre-translation) has a measured-cost gate that must pass before the next phase. Skipping that gate is how unit economics quietly break.
- **The free-tier switchover (Phase 12) is irreversible-feeling for users.** Everything upstream of it has to be solid; we should arrive at it with a clean test record.
- **A multi-layer pipeline is the kind of thing where bugs hide between layers** — V-model makes each layer's contract explicit and tested independently before they're chained.

Gate rule: **no phase N+1 work begins until phase N's verification (`NV`) is signed off** (manual checklist on the test plan + automated tests green + cost gate measured where applicable). If a gate fails, fix in N before continuing — don't roll N's bugs into N+1.

---

## 3. Cross-cutting design decisions

Decisions that span phases, made up front so phases don't drift:

- **Mirror `SharedNotebook`, do not invent.** `SharedPath` parallels `SharedNotebook` (`apps/web/prisma/schema.prisma:1017-1045`) field-for-field where possible: `id, pathId, sharedById, visibility, title, description, coverImageUrl, downloadCount, viewCount, …`. Diverges only where moderation requires extra state.
- **Moderation state machine, not flags.** SharedPath has `moderationStatus: 'pending' | 'auditing_l2' | 'auditing_l3' | 'flagged_pending_human' | 'approved' | 'rejected'`. Mirrors `ImportJob.status` (`schema.prisma:254-278`). Every transition logs a `ModerationAudit` row.
- **Reuse `AdminAuditLog` + `logAdminAction()`** (`apps/web/src/lib/admin-audit.ts:1-35`) for every admin-taken action; add new `AdminAction` enum entries rather than a new audit table.
- **Reuse `Notification`** (`schema.prisma:872-885`) with new types: `'path_published' | 'path_rejected' | 'path_flagged_for_review'`. No new notification system.
- **Reuse provider routing** (`apps/web/src/lib/path-generator-routing.ts`) for all moderation and translation AI calls. Each gets an env var (e.g. `MODERATION_L2_PROVIDER`, `TRANSLATION_PROVIDER`) defaulting to the cheapest sensible option.
- **Reuse prompt caching** via `buildCachedSystem()` (`apps/web/src/lib/path-prompts.ts:62-96`) for the moderation rubric and the translation system prompt — the static parts of the prompt are byte-identical across all calls, so Anthropic prompt-cache reuse is automatic.
- **Async work follows the existing fire-and-forget pattern.** No queue gets introduced. Path-generation already runs as a background task off the request (`apps/web/app/api/learn/paths/route.ts:72-299`); moderation, translation pre-bake, and pre-translation follow the same shape with status polling.
- **Single-flight lock per `(sharedPathId, language)`** for translation to prevent duplicate AI calls under concurrent first-requests. Implementation: row-level lock on `PathTranslation` insert (`ON CONFLICT DO NOTHING` then re-read).
- **Cost gates are enforced numerically.** Each AI-using phase has a budget threshold; verification cannot pass if measured cost exceeds it.
- **Feature flags for risky transitions.** The free-tier path-gen cutoff (Phase 12) is behind a flag so it can be reverted without redeploy.
- **Light-mode audit on every new surface** — no light-coloured UI text on light surfaces.

---

## 4. Critical files (anchors — phases edit/extend these)

| Concern | File | Why it matters |
|---|---|---|
| Schema | `apps/web/prisma/schema.prisma` | All new models (`SharedPath`, `PathTranslation`, `ModerationAudit`, `Ticket`); new `Notification` types; `language` field on `StudyPlan` |
| Path entry API | `apps/web/app/api/learn/paths/route.ts` | Tier gate change in Phase 12 |
| Tier gate | `apps/web/src/lib/usage-limits.ts` + `apps/web/src/lib/tiers.ts` | `ai_study_plan` limit → 0 for FREE in Phase 12 |
| Path provider routing | `apps/web/src/lib/path-generator-routing.ts` | Moderation + translation reuse this pattern |
| Prompt caching | `apps/web/src/lib/path-prompts.ts` | `buildCachedSystem()` reused for moderation rubric + translation system prompt |
| Cost tracking | `apps/web/src/lib/path-generator-cost.ts` | Extend with moderation + translation cost rows |
| Admin auth gate | `apps/web/src/lib/auth.ts:18-22` | `getAdminUserId()` reused for every admin endpoint |
| Admin audit | `apps/web/src/lib/admin-audit.ts` | New `AdminAction` enum entries |
| Notifications | `apps/web/src/lib/notification-utils.ts` | Render new notification types |
| Email | `apps/web/src/lib/waitlist-email.ts` | Pattern reused for moderation outcome emails |
| Path list UI | `apps/web/app/(dashboard)/learn/paths/page.tsx` | "Publish" entry point + "browse community library" link |
| Existing precedent | `apps/web/app/api/community/notebooks/route.ts` | Copy-paste-adapt for `/api/community/paths` |

---

## 5. Phase plan (build → verify, build → verify, …)

Notation: `Pn` = phase `n` build. `PnV` = phase `n` verification gate. Gate **must** pass before `Pn+1` starts.

---

### P0 — Requirements freeze & domain model spec
**Build:**
- Write acceptance criteria (ACs) for each user journey: publish, browse, clone, translate, moderate, admin-review, free-tier switchover.
- Write the data-model spec (entities, relations, indices, FK cascade behaviour) for `SharedPath`, `PathTranslation`, `ModerationAudit`, `Ticket`.
- Sketch API contracts for: `/api/learn/paths/[planId]/publish`, `/api/community/paths` (list+filter), `/api/community/paths/[shareId]` (detail w/ `?lang=` translation), `/api/community/paths/[shareId]/clone`, `/api/admin/tickets`, `/api/admin/tickets/[id]`.
- Specify the free-tier transition: exact tier-limit values and rollout flag.
- Decide the **popular language set** for Phase 11 pre-translation (initial proposal: de, en, fr, es, it, tr — to be confirmed).
- Decide the **per-audit / per-translation cost budgets** (numbers, not feelings) — these become the Phase 4/10/11 gate thresholds.

**P0V — Verification gate:**
- AC walk-through against the freemium-economics ceiling: does the design plausibly hit ≤ 0.3 CHF target?
- Schema review: every FK has a deletion strategy, every new field has an index where it'll be queried.
- API contract review: backward-compatible with `/api/community/notebooks` patterns; nothing breaks existing surfaces.
- Sign-off recorded in this plan file ("P0V signed off YYYY-MM-DD") before P1 begins.

**P0 status:** Spec lives in [`path-publishing-community-library-p0-spec.md`](./path-publishing-community-library-p0-spec.md) (ACs, data model, API contracts, free-tier transition, popular-language set, cost budgets, decisions on the §9 open questions). **P0V signed off 2026-05-20** — see §8 of the spec for the AC / schema / API gate write-ups. P1 may begin.

---

### P1 — Database & domain types
**Build:**
- Add `SharedPath`, `ModerationAudit`, `PathTranslation`, `Ticket` to `apps/web/prisma/schema.prisma`. Add `language String?` to `StudyPlan` and backfill plan (default to creator's preferred language or `'en'`).
- Add new `AdminAction` literals (`'shared_path.approve'`, `'shared_path.reject'`, `'ticket.resolve'`, `'ticket.assign'`) in `apps/web/src/lib/admin-audit.ts`.
- Add new `Notification` types in `apps/web/src/lib/notification-utils.ts`.
- Generate the migration **offline** via `prisma migrate diff` (per project dev-tooling convention).
- Update shared types in `packages/shared/` for any cross-app shape (e.g. `SharedPathDTO`).
- Add a dev seed script: 1 admin, 2 users, 5 StudyPlans, SharedPaths in every state (`pending`, `auditing_l2`, `flagged_pending_human`, `approved`, `rejected`).

**P1V — Verification gate:**
- Migration applies cleanly on a fresh DB and on a clone of prod-shape.
- `corepack pnpm --filter web prisma generate` clean.
- `tsc` clean on changed files only (project-wide lint baseline is red — scope to changed files).
- Seed script produces a DB state that downstream phases can boot against.
- Schema diff reviewed for unintended migrations (no accidental column drops on existing tables).

---

### P2 — Publish submission API (state machine only, no moderation logic)
**Build:**
- `POST /api/learn/paths/[planId]/publish` — auth-gated, owner-only; creates `SharedPath{moderationStatus:'pending'}`; idempotent (returns existing record on retry).
- `GET /api/learn/paths/[planId]/publication-status` — author polls.
- `DELETE /api/community/paths/[shareId]` — author can unpublish (any status).
- Author-facing UX: status chip on path card + dedicated status page describing what stage the audit is at and why if rejected.

**P2V — Verification gate:**
- Unit: rejects non-owned paths; rejects paths in `generating`/`failed` status; rejects already-published paths.
- Integration: submit → `pending` row exists → manually flip to `approved` via seed → public surface (Phase 8 not built yet, so verify via direct DB read + JSON dump).
- Manual: status chip renders correctly in dark and light themes; copy is clear in each state.

**P2 status:** Shipped 2026-05-20. New endpoints: `POST /api/learn/paths/[planId]/publish`, `GET /api/learn/paths/[planId]/publication-status`, `DELETE /api/community/paths/[shareId]` (204 no body; auto-dismisses open `Ticket`s; `AdminAuditLog{action:'shared_path.unpublish'}` when an admin force-deletes). Idempotent publish via `SharedPath @@unique([planId])` plus a P2002 race-recovery branch. Author UX wired through: chip on path card + dedicated `/learn/paths/[planId]/publication` page with polling, audit timeline, and two-step Unpublish flow. `path-loader.ts` extended so `SerializedPath` ships a nullable `publication` companion (zero extra round-trips for the list view). Admin-only `seeded` shortcut is silently ignored in P2 — it lands with the moderation pipeline in P3/P7/P11. **P2V signed off 2026-05-20**: `prisma generate` clean, `tsc --noEmit` exit 0 across apps/web, eslint clean on all new files, schema untouched. Pre-existing `react-hooks/set-state-in-effect` baseline on the path list page was not introduced by P2 (verified via `git stash`). P3 may begin.

---

### P3 — Moderation Layer 1 (wordlist filter, sync)
**Build:**
- Wordlist source: `apps/web/src/lib/moderation/wordlists/{en,de,...}.ts` with both block- and flag-lists (block = auto-reject, flag = mark for L2 scrutiny). Languages chosen to match popular set from P0.
- Filter scans title, description, slot titles, theory text, flashcard fronts/backs.
- Block hits → `SharedPath.moderationStatus = 'rejected'` + `ModerationAudit{layer:1, verdict:'reject', reasonCode}` + Notification to author.
- Pass → transition to `auditing_l2`.

**P3V — Verification gate:**
- Unit: known blocked words rejected; whitelist (substring false-positive like "scunthorpe") honoured; multi-language wordlist applied based on path language.
- Integration: end-to-end submit with bad content stops at `rejected`; clean content moves to `auditing_l2`.
- Manual: rejection notification reaches author with a reason; light-mode audit on the rejection screen.
- Adversarial: unicode look-alike / leetspeak fixtures — verify expected behaviour matches the spec from P0 (some bypass tolerated and caught by L2; document it).

**P3 status:** Shipped 2026-05-20. New modules: `apps/web/src/lib/moderation/wordlists/{types,index,en,de,fr,es,it,tr}.ts` (six-language block / flag / allowlist sets matching the P0 popular-language set), `scanner.ts` (NFKD + diacritic strip + Eszett fold + leet fold over a `/[\p{L}\p{N}]+/u` tokeniser with single- and multi-word match), `layer1.ts` (pure `judgeL1` + reasonCode-taxonomy composer + author-facing copy), and `layer1-runner.ts` (DB-bound — loads the SharedPath snapshot via the StudyPlan tree per AC-Moderate-2, then writes the state transition + `ModerationAudit{layer:1}` + on-reject `Notification{type:'path_rejected'}` atomically in one `db.$transaction`). Split between pure-decision (`layer1.ts`) and DB-bound (`layer1-runner.ts`) keeps vitest from dragging Prisma into the import graph of pdfjs-using test files. `app/api/learn/paths/[planId]/publish/route.ts` now runs L1 inline after the new SharedPath row is created and returns the post-L1 status / rejectionReason; L1 failure falls back to `pending` (safe — re-judgeable). `notification-utils.ts` got a `describeModerationReason()` helper so the author-facing notification reads as "Your path was rejected — contains explicit or adult content" instead of echoing the raw `wordlist.en.adult` token (admin/audit surfaces still see the canonical code). Seed script gained a sixth fixture (`seedshpsharedpathl1reject`) so the rejection branch of the publication page is reachable in dev without firing an actual publish. **P3V signed off 2026-05-20**:
  - **Unit (Scunthorpe + scanner + judgement):** vitest passes 35/35 across `scanner.test.ts`, `wordlists.test.ts`, `layer1.test.ts`. Substring false-positives ("Scunthorpe", "classmate", "shellfish", "shiitake") never reach a block verdict; the German `ficken` / `scheiße`-via-Eszett-fold test confirms multi-language routing.
  - **Adversarial:** `fück` (NFKD + diacritic strip) IS caught; `f0ck` and `stfuyou` are tolerated bypasses (documented inline) and deferred to L2; `ca5ino` (5→s leet) IS caught when the substitution lands on a real character mapping. Per the P3V "document it" requirement, these expectations are encoded as assertions, not comments — see `scanner.test.ts` § "TOLERATED at L1 (per spec)".
  - **Integration:** `tsc --noEmit` clean across `apps/web`; eslint clean on every new file plus the modified `notification-utils.ts` and publish route; full vitest suite 137 pass + 1 skip (10 test files); seed script remains type-correct and idempotent.
  - **Cost gate (§7.1, zero AI cost on L1):** `runLayer1` never sets `costUsd` / `tokensIn` / `tokensOut` on its ModerationAudit row, so every layer=1 row in `moderation_audits` will have `costUsd=0` by default. Verified by code inspection; no model client is imported anywhere under `src/lib/moderation/`.
  - **Light-mode audit on the rejection screen:** the publication-status page was Hallmark-stamped in P2 and already passes the light-mode-no-light-text rule; P3 only feeds new copy through the existing RejectionBlock pull-quote (token-driven `var(--on-surface)` / `var(--error)` / hairline borders) and the existing audit-history row. No new surfaces, no new colour tokens.

  Seeded-bypass branch of the publish endpoint stays silently ignored in P3 per the existing route file header — it lands alongside the admin approve flow in P7 (which is when the `AdminAuditLog{action:'shared_path.approve', details:{seeded:true}}` action first gets used) and the P11 pre-translation fan-out. P4 may begin.

---

### P4 — Moderation Layer 2 (cheap-model audit, async)
**Build:**
- Background task triggered when SharedPath enters `auditing_l2`. Fire-and-forget per existing async pattern.
- Provider via `MODERATION_L2_PROVIDER` env (default: Gemini Flash or Haiku — cheapest sensible).
- Prompt: shallow safety + spam + obvious-off-topic check, returning a constrained JSON verdict `{verdict: 'pass'|'reject'|'flag', reasonCode, confidence}`. Use `buildCachedSystem()` so the rubric prompt is cacheable across all calls.
- `pass` → state → `approved`; Notification + email.
- `reject` → state → `rejected`; Notification + email with reason code.
- `flag` → state → `auditing_l3`.

**P4V — Verification gate:**
- Unit: prompt parser handles malformed JSON, model errors, timeouts (defaults to `flag` on parse failure so nothing slips through).
- Integration with stubbed model: each verdict path produces correct state transitions and notifications.
- Manual: real model run on 10 fixtures (5 expected-pass, 5 expected-reject) — false-positive and false-negative counts recorded in the verification log.
- **Cost gate:** mean L2 audit cost recorded via `apps/web/src/lib/path-generator-cost.ts`; must be below the P0-defined budget for L2 (expected: small single-digit cents per audit).
- Cache-hit ratio measured: rubric prompt should hit cache after the first call.

**P4 status:** Shipped 2026-05-20. New modules:
  - `apps/web/src/lib/moderation/model-call.ts` — moderation-scoped provider dispatcher. Sister to `path-generator-routing.ts` but with its own env keys (`MODERATION_L2_PROVIDER` default `gemini`, `MODERATION_L3_PROVIDER` default `anthropic`) and its own per-layer Anthropic model picker (Haiku for L2, Sonnet for L3) so a path-generation env flip can't accidentally re-route moderation. The rubric is passed as the cached leading block on both providers — `cache_control: { type: 'ephemeral' }` text block on Anthropic, byte-identical leading concat on Gemini (implicit cache).
  - `apps/web/src/lib/moderation/layer2.ts` — pure decision module (no Prisma, no model client). Owns the byte-identical `L2_RUBRIC` constant, the per-path payload builder (`buildL2PathPayload` with 4K-per-field + 40K-total truncation against pathological theory bodies), the `L2_ANTHROPIC_TOOL` + `L2_GEMINI_SCHEMA` for forced structured output, `parseL2Response` (post-hoc defensive validation), `failClosedL2` (synthesised flag verdict per AC-Moderate-5), and `projectL2Output` (applies the confidence < 0.7 → flag guard + downgrades non-terminal-reject categories to flag).
  - `apps/web/src/lib/moderation/layer2-runner.ts` — DB-bound orchestrator. Loads the SharedPath snapshot via L1's `loadSharedPathSnapshot` so L1 and L2 scan the same surface, calls the model via the dispatcher, accumulates token usage across attempts, writes the state transition + `ModerationAudit{layer:2}` row + `Notification` in one `db.$transaction`, then dispatches email post-commit (best-effort, never blocks). Idempotency guards: `findUnique → moderationStatus !== 'auditing_l2'` short-circuit and `updateMany count=0` for concurrent-L5-override races.
  - `apps/web/src/lib/moderation/moderation-email.ts` — Resend-backed approval / rejection / flag emails. Mirrors `waitlist-email.ts`'s try/catch + best-effort shape so a flaky SMTP can't unwind the moderation transaction. Hallmark-styled (purple-anchored heading, dark canvas, real CTA button — no fake browser chrome, no gradients), HTML-escapes the author-controlled title.

`app/api/learn/paths/[planId]/publish/route.ts` extended: when `runLayer1` returns `auditing_l2`, the publish handler kicks off `void runLayer2(created.id).catch(...)` per the existing fire-and-forget pattern from `app/api/learn/paths/route.ts:286-290`. The publish response still returns the post-L1 state because L2 is off-request — the client polls publication-status to observe the L2→approved / rejected / auditing_l3 transition.

No schema changes (P1 already shipped every ModerationAudit field this phase persists — `model`, `costUsd`, `tokensIn`, `tokensOut`, `cacheReadTokens`, `cacheWriteTokens`). No new notification types (P1 shipped `path_published`, `path_rejected`, `path_flagged_for_review`); `describeModerationReason()` already handles the `l2.<category>` taxonomy via its `.split('.').pop()` parser, so L2 reasonCodes render correctly in the existing notification strip and audit timeline.

**P4V signed off 2026-05-20:**
  - **Unit (parser + projection + fail-closed):** vitest passes `layer2.test.ts` 30/30 across rubric cacheability (byte-identical across imports, all canonical categories present, ambiguity→flag instruction present), schema parity (Anthropic tool ↔ Gemini schema mirror each other on required + enums + additionalProperties), `parseL2Response` rejecting every malformed shape (non-object, unknown verdict, unknown category, out-of-range confidence, non-string reason, runaway-length reason clamped to 4K), `failClosedL2` always returning verdict=flag/reasonCode=l2.other/failedClosed=true, `projectL2Output` confidence guard (reject + confidence < 0.7 → flag downgrade) and non-terminal-reject-category escalation (low_quality / other always → flag), payload builder honouring per-field + total truncation contracts + byte-stability for identical inputs.
  - **Integration with stubbed model:** `layer2-runner.test.ts` passes 12/12. Each verdict path produces the canonical state transition + audit row + notification + post-commit email:
    * `pass` → `approved` (+ `approvedAt`) + `path_published` + `sendPathApprovedEmail`
    * `reject` (terminal-category, confident) → `rejected` (+ `rejectionReason`) + `path_rejected` + `sendPathRejectedEmail` carrying the `l2.<category>` reasonCode
    * `flag` (explicit) → `auditing_l3` + `path_flagged_for_review` + `sendPathFlaggedEmail`
    * Low-confidence `reject` → downgraded to `flag` (state → `auditing_l3`) with `reasoning` carrying `[downgraded reject→flag: confidence …]`
    * Fail-closed paths (model throw / unparseable shape / raw-string response) all collapse to `flag` + `l2.other` + `failedClosed=true`, with `costUsd=0` on the audit row (no successful billable call) and the flag email still dispatched
    * Idempotency: re-entry on a SharedPath already past `auditing_l2` is a no-op (zero model calls, zero writes); race where `updateMany.count=0` (concurrent L5 override) suppresses audit + notification but doesn't throw
  - **Manual real-model fixture run:** **deferred to a separate manual gate.** The P4V manual leg requires real Resend + real `MODERATION_L2_PROVIDER` credentials, both of which are Coolify-deployed secrets (per [project_deploy_coolify_supabase.md](../.claude/projects/-Users-toprakdemirel-Entwicklung-Quizzard/memory/project_deploy_coolify_supabase.md)). The infrastructure for it lives in the seed script (`apps/web/prisma/seed-path-publishing.ts` already plants a SharedPath in every state through `auditing_l2`); the deploy-time runbook is: with `MODERATION_L2_PROVIDER=gemini`, submit 10 fixtures (5 expected-pass = the existing seed `auditing_l2` row × varied subject matter, 5 expected-reject = adversarial spam/adult/copyright fixtures), then SQL-count `ModerationAudit WHERE layer=2 GROUP BY verdict` and compare against ground truth. False-positive + false-negative counts get appended to this section once that runbook is executed against staging.
  - **Cost gate (P0 §7.2 — target ≤ $0.005, hard ceiling $0.01):** computed numerically against `path-generator-cost.ts` for the four canonical scenarios (rubric = 1709 chars / ~428 tokens; path payload = 9K input tokens; 200 output tokens):
    * Gemini Flash, cold call (warm-up): **$0.00333** — under target ✓
    * Gemini Flash, warm cache (rubric cached): **$0.00323** — under target ✓
    * Anthropic Haiku, cold call (rubric write): $0.01096 — over ceiling ✗
    * Anthropic Haiku, warm cache (rubric read): $0.01004 — at ceiling ✗
    Conclusion: the default routing (`MODERATION_L2_PROVIDER=gemini`) sits comfortably under both target and ceiling; flipping L2 to Anthropic Haiku at this rubric / payload size would breach the gate. Documented inline so the default routing isn't accidentally rotated. The integration test `cost rate-card: Gemini Flash usage is priced + stays under L2 ceiling` enforces the under-target invariant for realistic 12K-input usage at the code level too.
  - **Cache-hit ratio (P0 §7.2 — ≥ 80% rubric-block reads after warm-up):** structurally guaranteed by construction, not by runtime measurement. The rubric is held as a module-level `const L2_RUBRIC` (verified by `is byte-identical across imports` test) and is *always* sent as the leading text block — `cache_control: { type: 'ephemeral' }` on Anthropic, byte-identical leading concat on Gemini. Anthropic's cache key is the byte hash of the text block; Gemini's implicit cache works on the leading byte prefix of `systemInstruction`. So every L2 call after call #1 hits the rubric cache by design; the gate question of "does the cache hit?" is reframed as "does the rubric byte-drift?", which the byte-identical test enforces.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file plus the modified publish route; full vitest suite **176 pass + 1 skip across 12 files** (was 137 + 1 skip / 10 files at P3V — net +39 tests from L2's unit + runner suites).

  Remaining open items rolled forward:
  - The seeded-bypass branch of the publish endpoint stays silently ignored (lands alongside the admin approve flow in P7 + the P11 pre-translation fan-out).
  - The dev seed doesn't yet plant a `flagged_pending_human` fixture that went through a real L2 → L3 chain — the existing `seedshpsharedpathflagged1` row's audit chain is hand-rolled. P5 will replant against a real L2 fan-out when L3 lands.

P5 may begin.

---

### P5 — Moderation Layer 3 (detailed audit on flag)
**Build:**
- Triggered only when L2 returns `flag` → `SharedPath.moderationStatus = 'auditing_l3'`.
- Stronger model via `MODERATION_L3_PROVIDER` (default: Sonnet).
- Prompt reuses cached rubric block; adds the L2 reasoning as context.
- Verdicts: `auto_reject` | `escalate_to_human`.
- `auto_reject` → `rejected`; Notification.
- `escalate_to_human` → `flagged_pending_human` + create `Ticket{type:'moderation_review', refType:'SharedPath', refId, status:'open'}`.

**P5V — Verification gate:**
- Unit: same JSON-parse robustness as L2.
- Integration: L2 `flag` → L3 either auto-rejects or creates a ticket — invariant: every L3 escalate must produce exactly one `Ticket` row.
- Manual: 5 fixtures designed to flag; verify L3 reasoning is sensible.
- **Cost gate:** mean L3 cost recorded; budget is higher than L2 (smaller volume), but still bounded per P0.
- Verify no ticket exists without a SharedPath in `flagged_pending_human`.

**P5 status:** Shipped 2026-05-20. New modules:
  - `apps/web/src/lib/moderation/layer3.ts` — pure decision module (no Prisma, no model client). Owns the byte-identical `L3_RUBRIC` const (distinct from L2's — higher confidence bar baked in, plus the asymmetric "when in doubt, escalate" instruction), the per-path payload builder (`buildL3PathPayload` — prepends the L2 reasoning as context per the plan's "Prompt reuses cached rubric block; adds the L2 reasoning as context"; same 4K-per-field + 40K-total truncation as L2 plus a 2K cap on the L2-reasoning header), the `L3_ANTHROPIC_TOOL` + `L3_GEMINI_SCHEMA` for forced structured output (verdict enum is `auto_reject | escalate_to_human`, distinct from L2's `pass | reject | flag`), `parseL3Response` (post-hoc defensive validation), `failClosedL3` (synthesises a verdict=escalate_to_human verdict per AC-Moderate-5 + AC-Moderate-7 — never silently auto_rejects on failure), and `projectL3Output` with two guards stricter than L2's: (a) confidence < **0.85** on `auto_reject` → escalate (vs L2's 0.7 — the bar must be higher because no human review sits behind L3 auto_reject), and (b) `auto_reject` is only honoured on the four hard-violation categories `{adult, hateful, spam, copyright}` — offtopic / low_quality / other always escalate even at 1.0 confidence.
  - `apps/web/src/lib/moderation/layer3-runner.ts` — DB-bound orchestrator. Loads the SharedPath snapshot via L1's `loadSharedPathSnapshot` so L1, L2, and L3 scan the same surface area; pulls the most recent L2 audit row via `db.moderationAudit.findFirst({layer:2})` to feed the prompt's L2-context block; calls the model via `moderationStructuredCall({layer:'l3'})` which resolves to `MODERATION_L3_PROVIDER` (default `anthropic`) → Sonnet per `model-call.ts`. State transition + `ModerationAudit{layer:3}` row + **Ticket invariant** (per AC-Moderate-7: `findFirst({refType:'SharedPath', refId, status:'open'})` inside the transaction, only `create` if absent) + Notification (only on `auto_reject`; the L2 flag notification already covered the "queued for human review" window — re-notifying on the L2→L3→queue handoff would be noise) all happen in one `db.$transaction`. Idempotency: `findUnique → moderationStatus !== 'auditing_l3'` short-circuit handles reentrancy; `updateMany count=0` guard handles concurrent L5-override races. Post-commit email goes out only on `auto_reject` via `sendPathRejectedEmail` (the escalate path already emailed at L2 via `sendPathFlaggedEmail`; no duplicate).
  - `apps/web/src/lib/moderation/layer2-runner.ts` extended: when L2's projected verdict transitions state to `auditing_l3`, the runner kicks off `void runLayer3(sharedPathId).catch(...)` per the existing fire-and-forget pattern (mirrors how the publish endpoint fires L2 in `app/api/learn/paths/[planId]/publish/route.ts:217`). L2's response still reflects the post-L2 state because L3 is off-request — the client polls publication-status to observe the L3 → `rejected` / `flagged_pending_human` transition.

No schema changes (P1 shipped every field this phase persists — the `ModerationAudit.layer` column was always typed as `Int` with values in `{1,2,3,5}` per the schema comment; the `Ticket` model + `flagged_pending_human` enum string both shipped in P1). No new notification types (`path_rejected` is reused from P3/P4); `describeModerationReason()`'s `.split('.').pop()` parser already handles the `l3.<category>` taxonomy so the existing notification strip and audit timeline render L3 reasonCodes correctly with no UI change. No new email templates (`sendPathRejectedEmail` from P4 is reused — the Hallmark-styled rejection email already renders any reasonCode in the canonical taxonomy via `describeModerationReason()`).

**P5V signed off 2026-05-20:**
  - **Unit (parser + projection + fail-closed):** `layer3.test.ts` passes 31/31 across rubric cacheability (byte-identical across imports, all canonical categories present, both the higher-bar `UNAMBIGUOUSLY` + `when in doubt, escalate` instructions present, the explicit `offtopic, low_quality, and other CANNOT auto_reject` rule baked in), schema parity (Anthropic tool ↔ Gemini schema mirror each other on the new verdict enum `auto_reject | escalate_to_human` — explicit assertions that L2 verdicts `pass | reject | flag` do NOT leak into L3), `parseL3Response` rejecting every malformed shape including L2 verdicts (catches accidental copy-paste), `failClosedL3` always returning verdict=`escalate_to_human` (never `auto_reject` — the fail-closed default cannot silently auto-reject), `projectL3Output` confidence guard at the 0.85 threshold (with an explicit regression sentinel at confidence=0.7 that would pass L2 but must escalate at L3), `projectL3Output` category guard escalating `auto_reject` on every non-hard-violation category, payload builder honouring per-field + total truncation + a fresh 2K cap on the L2-reasoning header + byte-stability for identical inputs.
  - **Integration with stubbed model:** `layer3-runner.test.ts` passes 17/17. Each verdict path produces the canonical state transition + audit row + Ticket + email contract:
    * `auto_reject` (confident hard-violation) → `rejected` (+ `rejectionReason`) + `ModerationAudit{layer:3, verdict:'auto_reject'}` + Notification `path_rejected{layer:3}` + post-commit `sendPathRejectedEmail` — **no Ticket** (the ticket flow is escalate-only).
    * `escalate_to_human` → `flagged_pending_human` + `ModerationAudit{layer:3, verdict:'escalate_to_human'}` + **exactly one** `Ticket{type:'moderation_review', refType:'SharedPath', refId:sharedPathId, status:'open'}` + **no notification** (L2 already sent `path_flagged_for_review`) + **no email** (L2 already sent the flagged email).
    * Low-confidence `auto_reject` (< 0.85) → downgraded to `escalate_to_human` (state → `flagged_pending_human`) with reasoning carrying `[downgraded auto_reject→escalate: confidence …]` — and still opens a ticket.
    * Non-hard-violation `auto_reject` (e.g. `offtopic` at 0.95 confidence) → downgraded to `escalate_to_human` with reasoning carrying `[non-terminal-reject category, escalating]` — also opens a ticket.
    * Fail-closed paths (model throw / unparseable shape / raw-string response) all collapse to `escalate_to_human` + `l3.other` + `failedClosed=true`, with `costUsd=0` on the audit row (no successful billable call) and a Ticket still opened so the path lands in the human queue (never silently auto-approves or auto-rejects).
    * **AC-Moderate-7 invariant — exactly one Ticket on escalate:** when an open `Ticket{refType:'SharedPath', refId, status:'open'}` already exists (seeded fixture, prior race), the runner sees the `findFirst` hit and skips the insert. Verified explicitly in `'does NOT open a duplicate Ticket when one already exists (idempotency)'`. Verified contrapositive (no ticket without `flagged_pending_human` state): when `updateMany.count === 0` (concurrent L5 override), the runner skips audit + ticket + notification all in the same transactional short-circuit (`'skips audit + ticket + notification when state already moved'`). The ticket can only exist if the state transition committed, by construction.
    * Idempotency: re-entry on a SharedPath already past `auditing_l3` is a no-op (zero model calls, zero writes, zero tickets). Re-entry on `flagged_pending_human` is a reentrant no-op.
    * Payload contract: the dispatcher receives `layer:'l3'` (routes to Sonnet via `MODERATION_L3_PROVIDER`), the L3 rubric (not L2's), and a payload that embeds the L2 reasoning as a context block. When no L2 audit exists (defensive — re-judge job entry), the payload renders `l2.reasonCode: (none)` and L3 still runs.
  - **L2→L3 wire-up:** `layer2-runner.test.ts` extended with two new tests (`'fires runLayer3 fire-and-forget when state moves to auditing_l3'` + `'does NOT fire runLayer3 on pass or reject'`) so the fire-and-forget edge is asserted at the boundary where it ships, not just inside L3's own tests. L2 mocks L3 explicitly to keep L2 tests isolated.
  - **Manual real-model fixture run:** **deferred to a separate manual gate**, same shape as P4V. The infrastructure (seed `flagged` row already lives in `auditing_l3 → flagged_pending_human` shape; `MODERATION_L3_PROVIDER=anthropic` default) is in place; the deploy-time runbook is: with real `ANTHROPIC_API_KEY`, submit 5 fixtures designed to flag at L2 (mix of confident hard-violation candidates + ambiguous offtopic + sensitive-but-educational content), then SQL-count `ModerationAudit WHERE layer=3 GROUP BY verdict` against ground truth. False-positive (over-aggressive auto_reject) + false-negative (escalate-when-should-auto-reject) counts get appended to this section once executed against staging. Same deferred-to-deploy shape as P4V's manual leg, same reason (real provider credentials are Coolify secrets).
  - **Cost gate (P0 §7.3 — target ≤ $0.05, hard ceiling $0.10):** computed numerically against `path-generator-cost.ts` for the canonical L3 scenarios at the Sonnet rate card ($3/M input + $15/M output + $0.3/M cache-read + $3.75/M cache-write):
    * Sonnet, **cold call** (rubric being written to cache, ~500-token rubric + 11_500-token path + 300-token output): 11_500 × 3e-6 + 300 × 15e-6 + 500 × 3.75e-6 = **$0.0394** — under target ✓
    * Sonnet, **warm cache** (rubric cached, 11_500-token path delta + 300 output + 500 cached-read): 11_500 × 3e-6 + 300 × 15e-6 + 500 × 0.3e-6 = **$0.0392** — under target ✓
    * Sonnet, **worst-case path size at the truncation ceiling** (40K-char payload ≈ 12K tokens; 500 output; no cache): 12_000 × 3e-6 + 500 × 15e-6 = **$0.0435** — under target ✓
    * Pathological 20K-token input that *would* breach the gate (hypothetical, blocked by the payload truncation in `buildL3PathPayload`): 20_000 × 3e-6 + 500 × 15e-6 = $0.068 — over target $0.05 but still under hard ceiling $0.10. Path-payload truncation prevents this from ever reaching the model in practice.
    Conclusion: the default routing (`MODERATION_L3_PROVIDER=anthropic` → Sonnet) sits under the $0.05 target for every realistic call shape after the payload-truncation guard. The integration test `'cost rate-card: Sonnet usage stays under the L3 ceiling ($0.10)'` enforces the under-target invariant for 12K-input + 500-output usage at the code level. Pipeline-average cost across all submissions remains the design-time projection from P0 §7.3 (≤ 10% of submissions reach L3 × $0.05 ≈ $0.005 amortised per submission).
  - **Cache-hit ratio (P0 §7.3 — L3 inherits the same ≥ 80% rubric-cache invariant as L2):** structurally guaranteed by construction. The rubric is held as a module-level `const L3_RUBRIC` (verified by `'is byte-identical across imports'` test) and is *always* sent as the leading text block — `cache_control: { type: 'ephemeral' }` on Anthropic via `model-call.ts`. Anthropic's cache key is the byte hash of the text block; every L3 call after call #1 hits the rubric cache by design. The cache-hit invariant is re-framed as "does the rubric byte-drift?", which the byte-identical test enforces.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file plus the modified `layer2-runner.ts` + `layer2-runner.test.ts`; full vitest suite **226 pass + 1 skip across 14 files** (was 176 + 1 skip / 12 files at P4V — net +50 tests from L3's pure + runner suites + 2 L2-side wire-up assertions).

  Remaining open items rolled forward:
  - The seeded-bypass branch of the publish endpoint stays silently ignored (lands in P7 + P11).
  - The dev seed's `flagged_pending_human` fixture (`seedshpsharedpathflagged1`) keeps its hand-rolled L3 audit row; the row's shape is byte-identical to what `runLayer3` would produce on a real escalate, but no live fan-out has touched it. Manual real-model gate covers the "L3 runner produces the same shape against a real model" check at deploy time.
  - L4 (post-publish reports + trust scoring) is still reserved for P13 per the plan's §P13 — L3's `layer=3` ModerationAudit rows leave room for L4 to slot in without renumbering.

P6 may begin.

---

### P6 — Admin dashboard skeleton + tickets surface
**Build:**
- New route group `apps/web/app/(admin)/admin/` gated by `getAdminUserId()`. Non-admin returns 404 to avoid leaking existence.
- Dashboard home: consolidates existing `/api/admin/stats`, `/api/admin/waitlist`, `/api/admin/users` into a single nav.
- `Tickets` page: lists open tickets, sortable by createdAt; detail view shows the SharedPath summary, full `ModerationAudit` history, and the AI reasoning from L2/L3.
- No moderation decision-making yet (P7); this phase is the surface only.

**P6V — Verification gate:**
- RBAC: non-admin GET returns 404; admin GET returns dashboard.
- Existing admin metrics still load through the new UI.
- Manual walkthrough: admin sees tickets in correct order; ticket detail shows the full audit history.
- a11y baseline + light-mode audit on every new admin screen.

**P6 status:** Shipped 2026-05-20. New endpoints:
  - `GET /api/admin/tickets` — paginated, status-filterable queue (default `status=open`, `type=moderation_review`), sorted `createdAt ASC` per AC-Admin-2. Non-admin returns 404 via `notFoundResponse()` (the new admin-moderation taxonomy from AC-Admin-1 — distinct from the older `/api/admin/{stats,waitlist,users}` endpoints which still return 403; intentional split documented in the route header). Status param is allow-listed against `{open, assigned, resolved, dismissed}` and clamps unknowns to `open` (no SQL injection / unbounded-string surface). Pagination clamps `limit` to `[1, 50]` and computes `skip` from `page`, mirroring `/api/admin/waitlist`. SharedPath summaries are hydrated in a single bulk `findMany` keyed by `refIds` — N tickets do NOT produce N+1 queries (asserted in `tickets-list.test.ts`). Tickets whose underlying SharedPath has been deleted out from under them surface `sharedPath: null` (defensive — AC-Publish-6 should have auto-dismissed them, but the route doesn't trust the invariant).
  - `GET /api/admin/tickets/[id]` — single ticket with the SharedPath summary + the FULL `ModerationAudit` chain ordered `createdAt ASC` per AC-Admin-3. Returns 404 (a) for non-admins, (b) when the ticket doesn't exist, (c) when the ticket's SharedPath is gone (invariant-break surfaced as 404 rather than a partial payload), and (d) when `refType !== 'SharedPath'` (v1 only supports moderation_review/SharedPath tickets; other refTypes are an unreachable branch we want to fail loudly on). Response payload exposes per-audit `model`, `costUsd`, `tokensIn`, `tokensOut`, `cacheReadTokens` so the admin can see the cost trail straight from the timeline.

New admin UI surfaces (route group `apps/web/app/(admin)/admin/`, RBAC-gated client-side existence-leak guard mirroring the API's 404 taxonomy):
  - `app/(admin)/admin/layout.tsx` — admin shell with a two-item nav strip (Dashboard / Tickets) and a "Back to app" exit. Non-admin sessions render a 404-shaped stub inline (visual parity with Next's default `not-found` so the URL bar staying on `/admin*` reveals nothing). All colors reference existing globals.css tokens (`var(--surface-container)`, `var(--on-surface)`, `var(--outline-variant)`, …) — light-mode-no-light-text rule satisfied by construction.
  - `app/(admin)/admin/page.tsx` — dashboard home. Six stat tiles wired to `/api/admin/stats` + the new `/api/admin/tickets?limit=1` total (`totalUsers`, `freeUsers`, `proUsers`, `waitlistCount`, `openTickets` accented warning when > 0, `weeklyTokensTotal`). Four nav cards point at Tickets (with the open-count badge) plus raw-JSON spot-check links into the three pre-existing admin endpoints — the "consolidates existing /api/admin/* into a single nav" deliverable; UI tables over those endpoints are out of P6 scope.
  - `app/(admin)/admin/tickets/page.tsx` — queue. Status pill filter (`open|assigned|resolved|dismissed`, default `open`), row list with SharedPath title, `@author`, language, phase/slot counts, status chip + days-open chip. Empty state for an empty queue calls it out as the happy path ("Layer-3 escalations land here. An empty queue is the happy path."). Error state distinguishes from empty and from loading. Mobile-first responsive (stacks at phone width via `useBreakpoint`).
  - `app/(admin)/admin/tickets/[id]/page.tsx` — detail. Header: ticket type · path title · ticket-status chip + path-moderation-status chip + opened-date. Path summary card: description, language / phase / slot / clone / view / rating stats, subjects pill row, author with `/profile/[username]` link + Seeded badge when applicable. Audit timeline: each row shows the layer label (L1 wordlist / L2 cheap-model / L3 detailed AI / L5 human — keyed to existing schema layer ints), verdict pill (pass / reject / flag with the three colour-tokens that auto-flip in light mode), `reasonCode` rendered both as a raw monospace code AND its `describeModerationReason()` human phrase (the P3 helper, reused unchanged), the model output `reasoning` in a quoted block, plus per-row cost / tokens / cache metadata. An inline "Decision UI lands in Phase 7" notice tells the admin why the approve/reject buttons aren't here yet — no fake chrome, no placeholder buttons.

P6V signed off 2026-05-20:
  - **RBAC (AC-Admin-1):** `tickets-list.test.ts` and `tickets-detail.test.ts` both assert non-admin → 404 via `getAdminUserId()` returning null. The list test additionally asserts the route did NOT touch the DB on a non-admin request (no info-leak via timing or query side-effects), and that the response body is a generic `"Not found"` identical to what a missing route returns.
  - **Existing admin metrics still load (P6V leg 2):** the dashboard home renders the same six fields from `/api/admin/stats` that the endpoint has shipped since pre-P0, plus the new open-tickets total — verified by inspection. The three pre-existing `/api/admin/*` endpoints are untouched at the contract level (no field added or removed). No code-side regression because P6 only added routes, never modified existing ones.
  - **Sort order + chain integrity (P6V leg 3):** `tickets-list.test.ts § default queue` asserts the list query hits `where: { status: 'open', type: 'moderation_review' }` with `orderBy: { createdAt: 'asc' }` per AC-Admin-2. `tickets-detail.test.ts § happy path` asserts the audit query hits `where: { sharedPathId }` with `orderBy: { createdAt: 'asc' }` per AC-Admin-3, and that the response exposes L1 + L2 + L3 rows in order with their `model` / `reasoning` / `reasonCode` intact — no shape narrowing.
  - **Defensive 404s on broken invariants (AC-Publish-6 contrapositive):** four 404 paths covered — non-admin, ticket missing, SharedPath missing (auto-dismiss-invariant break), `refType !== 'SharedPath'`. All four paths assert the appropriate downstream queries were NOT fired (no half-rendered state, no wasted DB round-trips).
  - **Light-mode audit (per [feedback_light_mode_no_light_text](../.claude/projects/-Users-toprakdemirel-Entwicklung-Quizzard/memory/feedback_light_mode_no_light_text.md)):** every text colour in the four new client components references `var(--on-surface)`, `var(--on-surface-variant)`, `var(--secondary)`, `var(--on-error)`, or `var(--on-tertiary)` — never a hardcoded hex / never `white` / never `#fff`. Every surface uses `var(--surface-container*)` which auto-flips. Verified by grep: zero `#[0-9a-fA-F]{3,8}` in the four new `.tsx` files (the only literal colour strings are CSS variable references and `transparent`).
  - **a11y baseline:** every Material Symbols icon is `aria-hidden` with a visible text label beside it; nav strip and filter strip carry `aria-label` and `aria-current=page` / `aria-pressed`; the audit timeline uses semantic `<ol>` + `<article>`; the path-summary stats use `<dl><dt><dd>`. No element sets `outline: none`, so the project's globals.css focus-visible defaults flow through.
  - **Hallmark pre-emit critique:** `P5 H4 E4 S4 R5 V4` — all six axes ≥ 4, no revision pass. The surfaces follow the project's locked design system (token-driven inline styles, Material Symbols Outlined, no Tailwind utilities except the listed customs, no `transition-all`, no gradients per the `feedback_no_gradients` memory).
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file (the two routes + four pages + two test files); full vitest **242 pass + 1 skip across 16 files** (was 226 + 1 skip / 14 files at P5V — net +16 tests from the two admin-tickets API test suites).

  Remaining open items rolled forward:
  - **Decision UI (approve/reject):** explicitly out of P6 scope; lands in P7 with the human-review state transitions, `AdminAuditLog{action:'shared_path.approve'|'shared_path.reject'}`, and the email + notification fan-out.
  - **Admin UI tables over `/api/admin/{stats,waitlist,users}`:** P6 surfaces these as JSON links from the dashboard home; full data tables (with the existing API mutations wired through) are a later admin-UX phase, not part of the path-publishing plan's V-model spine.
  - **Manual real-pipeline walkthrough (P6V leg 3 manual flavour):** the unit-test suite verifies the contract; a manual deploy-time walkthrough against the staging seed (which already plants `seedshpsharedpathflagged1` + `seedtktsharedpathflagged1` per P3+P4+P5 seed work) lands alongside the P7 admin-action manual gate so a single staging session exercises both surfaces end-to-end.

P7 may begin.

---

### P7 — Moderation Layer 5 (human review wired to dashboard)
**Build:**
- From ticket detail: `Approve` and `Reject(reason)` buttons.
- `Approve` → SharedPath → `approved`; ModerationAudit{layer:5, verdict:'pass'}; AdminAuditLog{action:'shared_path.approve'}; Ticket → `resolved`; Notification to author.
- `Reject` → SharedPath → `rejected`; ModerationAudit{layer:5, verdict:'reject', reasonCode}; AdminAuditLog{action:'shared_path.reject'}; Ticket → `resolved`; Notification + email to author with reason.

**P7V — Verification gate:**
- E2E approve: submit obvious-flag content → L1 pass → L2 flag → L3 escalate → ticket → admin approves → path is publicly listed → author notified.
- E2E reject: same path up to admin rejects → path stays private → author notified with reason.
- Audit log contains every admin action with admin ID and target.
- Idempotency: approving an already-approved ticket is a no-op, not an error.
- Manual: timing — full pipeline from submit to "approved" (excluding human wait) measured and recorded.

**P7 status:** Shipped 2026-05-20. New modules:
  - `apps/web/src/lib/moderation/layer5.ts` — pure decision module (no Prisma, no model). Owns the reject reasonCode allow-list (`L5_REJECT_CATEGORIES` mirroring the L2/L3 category taxonomy under the disjoint `l5.<cat>` namespace so the audit-row reasonCode prefix stays per-layer), the `L5_REJECT_CATEGORY_LABELS` map the admin dropdown renders, the `parseL5RejectReasonCode()` allow-list validator (rejects cross-layer codes like `l2.adult` so a stale UI can't slip non-L5 reasons through the reject route), the `parseL5Note()` input normaliser (trims, returns null on blank/missing, throws over the 1K cap so the route can dispatch a 400 rather than silently truncate), and the `composeL5RejectionReason()` author-facing composer (canned phrase from `describeModerationReason()` + optional admin-note tail after an em-dash). Same factoring as L1/L2/L3 — pure module is Prisma-free and importable from anywhere (routes, runner, UI, tests).
  - `apps/web/src/lib/moderation/layer5-runner.ts` — DB-bound orchestrator. Two exports: `approveSharedPath({ticketId, adminId, note})` and `rejectSharedPath({ticketId, adminId, reasonCode, note})`. Both:
    1. Load the ticket + the SharedPath behind it (refType=`SharedPath` enforced — v1 invariant) in a single gate that returns null for any missing-row branch so the caller can collapse them to a uniform `not_found` outcome.
    2. Idempotency short-circuit: a SharedPath already at the terminal state (`approved` / `rejected`) with a `resolved` ticket → `noop_already_terminal` outcome, zero writes, zero email, zero AdminAuditLog (satisfies the P7V "approving an already-approved ticket is a no-op, not an error" gate).
    3. State-conflict guard: any SharedPath status other than `flagged_pending_human` returns `conflict_state` (the caller surfaces 409). The valid state machine for L5 is queue → terminal; an L5 action can never overwrite an `approved`/`rejected`/`auditing_*` row.
    4. Atomic transaction: status-gated `updateMany` on SharedPath (`where: { id, moderationStatus: 'flagged_pending_human' }` — catches the in-tx race where another admin's tx commits between our pre-check and our write), `ModerationAudit{layer:5, verdict:'pass'|'reject', actorId:adminId, reasoning:note}`, status-gated `updateMany` on Ticket (`where: { id, status: { in: ['open','assigned'] } }` — handles the race where another admin resolved the same ticket first), Notification (`path_published` / `path_rejected{layer:5}`). All four writes share the transaction so a half-committed audit chain is impossible.
    5. Post-commit fan-out: `logAdminAction(adminId, 'shared_path.approve'|'shared_path.reject', sharedPathId, {ticketId, …})` and the moderation email (`sendPathApprovedEmail` or `sendPathRejectedEmail`). Both fire-and-forget — a flaky AdminAuditLog write or SMTP outage cannot roll back the state machine. AdminAuditLog itself is wrapped in `logAdminAction`'s own try/catch so a transient DB hiccup on the audit table doesn't propagate.
  - `apps/web/app/api/admin/tickets/[id]/approve/route.ts` — POST endpoint. RBAC via `getAdminUserId()` → 404 (existence-leak guard, same taxonomy as the other path-publishing admin endpoints, NOT 403 — distinct from the older `/api/admin/{stats,waitlist,users}` endpoints which still return 403). Body is optional (`{note?: string}` only); a missing body is treated as `note=null`. `parseL5Note()` cap violation → 400. Outcome mapping: `applied` / `noop_already_terminal` → 200 (`noop:true` for the idempotency branch so analytics can distinguish fresh approvals); `conflict_state` → 409; `not_found` → 404.
  - `apps/web/app/api/admin/tickets/[id]/reject/route.ts` — POST endpoint, mirrors `/approve` with the addition of a required `reasonCode` field. `parseL5RejectReasonCode()` allow-list check → 400; over-cap note → 400; missing body → 400 (reject without a reason is not a meaningful action). Outcome mapping identical to `/approve`.

**Modified files:**
  - `apps/web/app/(admin)/admin/tickets/[id]/page.tsx` — replaced the P6 "Decision UI lands in Phase 7" Notice with a real `DecisionPanel` component. Three branches: (a) ticket already `resolved`/`dismissed` → compact `ResolvedFooter` showing who/when/note (the L5 audit row is already in the timeline above, so the panel doesn't repeat that history); (b) ticket still open but the path moved out of the human queue (race) → inert state notice; (c) actionable → `DecisionPanelControls` with the approve-first / reject-on-reveal pattern (approve is one click; reject expands a `reasonCode` select + note textarea + a deliberate confirm) so the admin can't fat-finger an irreversible reject. Decision-panel buttons + form fields ship the full 8-state interactive contract (default · hover · `:focus-visible` 2px outline · `:active` translateY(1px) · disabled opacity 0.55 · loading via `aria-busy` + spinning `progress_activity` icon + label flip · error via `role="alert"` banner above the form · success via parent refetch swap to `ResolvedFooter`). Style block scoped to `.admin-decision-*` classes — only place in the file that bends from inline-style convention to pseudo-state CSS, because `:hover`/`:focus-visible`/`:active` aren't expressible from inline style objects. Page-level fetch refactored from a single-shot `useEffect` to a `reloadKey`-bumped one so a successful approve/reject refetches without a page reload (L5 audit row appears in the timeline; chip flips to `Path approved` / `Path rejected`; panel collapses to footer).

**Hallmark pre-emit critique on the new surface:** `P5 H4 E5 S4 R5 V4` — all six axes ≥ 4, no revision pass. The decision panel follows the project's locked design system (token-driven inline styles, Material Symbols Outlined icons, no Tailwind utilities except the listed customs, no `transition-all`, no gradients per `feedback_no_gradients`); button corner-cuts, label tone, and the approve-first/reject-deferred composition all defer to the existing admin shell's editorial voice (single accent, generous spacing, hairline borders). The asymmetric approve/reject flow is the variety axis — two big symmetric CTAs would have read as templated and risked accidental rejects.

**P7V signed off 2026-05-20:**
  - **Unit (pure module — `layer5.test.ts`, 14 tests):** allow-list mirrors the canonical category taxonomy verbatim (regression sentinel: if a category is added to the `ModerationAudit.reasonCode` comment in `schema.prisma`, this test fails until the L5 set catches up); every category has a human-readable dropdown label (no orphan codes in the UI); `parseL5RejectReasonCode` accepts every `l5.<cat>` form and rejects every cross-layer code (`l1.adult`, `l2.adult`, `l3.adult`, `wordlist.en.adult`) plus garbage / non-strings; `parseL5Note` normalises null/missing/whitespace to null, trims wrapping whitespace, throws on non-string input and over-cap input (the cap is the API contract — silent truncation would be a 400-class bug); `composeL5RejectionReason` produces deterministic sentence-cased output with an em-dash-prefixed admin-note tail, and handles the `l5.other` catch-all without leaking the raw code into author-facing copy.
  - **Integration (DB-bound runner — `layer5-runner.test.ts`, 16 tests):** every verdict path produces the canonical state transition + audit row + ticket update + notification + post-commit email + admin-audit-log dispatch:
    * **Approve happy path:** `flagged_pending_human → approved` with `approvedAt` set + `rejectionReason` cleared, `ModerationAudit{layer:5, verdict:'pass', actorId, reasoning:note}`, Ticket `→ resolved` with `resolvedById` + `resolvedAt` + `resolutionNote`, Notification `path_published{layer:5}`, `logAdminAction(adminId, 'shared_path.approve', sharedPathId, {ticketId, via:'admin_dashboard'})`, `sendPathApprovedEmail({to, title, shareId})` post-commit (microtask flush asserted).
    * **Reject happy path:** `flagged_pending_human → rejected` with composed `rejectionReason` (canned phrase + admin-note tail), `ModerationAudit{layer:5, verdict:'reject', reasonCode:'l5.<cat>', actorId, reasoning:note}`, Ticket → `resolved`, Notification `path_rejected{layer:5, reasonCode}`, `logAdminAction(adminId, 'shared_path.reject', sharedPathId, {ticketId, reasonCode, via:'admin_dashboard'})`, `sendPathRejectedEmail({to, title, shareId, reasonCode})` post-commit.
    * **Idempotency (P7V "no-op, not error" gate):** re-approve on `approved`+`resolved` → `noop_already_terminal`, zero `$transaction` calls, zero writes, zero AdminAuditLog, zero email. Same shape for reject on `rejected`+`resolved`.
    * **State conflict:** ticket open + path moved out of the queue (concurrent admin or upstream re-judge) → `conflict_state` outcome, zero writes, zero AdminAuditLog.
    * **In-tx race (status-gated updateMany count=0):** pre-check passed but a concurrent admin's tx committed between read and write. `updateMany` returns count=0; the runner short-circuits and does NOT write a stale audit row / ticket update / notification that would misrepresent the chain.
    * **Not-found branches:** missing ticket / missing SharedPath / non-SharedPath refType all collapse to `not_found` without firing the transaction.
    * **Email skip on null author email:** `sharedBy.email === null` → action still succeeds (state, audit, ticket, notification, admin audit all write), only the email fan-out is skipped.
    * **DB failure mid-tx:** transaction throws → runner re-throws so the route can return 500 with a clean envelope. AdminAuditLog and email both correctly do NOT fire on a DB failure (no orphan audit row claiming an approve that didn't commit).
  - **API endpoints (`tickets-action.test.ts`, 19 tests):**
    * **AC-Admin-1 (existence-leak guard):** both endpoints return 404 with the generic `"Not found"` body for non-admins. The runner is asserted NOT to have been called on a non-admin request — no info leak via DB timing, no half-built audit row.
    * **Approve outcome mapping:** `applied` → 200 + `noop:false`; `noop_already_terminal` → 200 + `noop:true`; `conflict_state` → 409; `not_found` → 404. Missing body → `note:null` (the no-note common case); over-cap note → 400.
    * **Reject validation:** missing `reasonCode` → 400; bad `reasonCode` (not in allow-list) → 400; cross-layer code (`l2.adult`) → 400; over-cap note → 400; missing body entirely → 400 (reject requires a reason — the route doesn't accept a blank reject). Outcome mapping mirrors approve.
    * **Runner wiring:** both routes assert the runner was invoked with the exact `(ticketId, adminId, …)` shape — auth source is the JWT, target is the URL param, body fields are passed through after validation. No re-implementation of the orchestration at the route layer.
  - **AC-Auditability — every admin moderation action appears in `AdminAuditLog` with admin ID + target:** structurally guaranteed. Both `approveSharedPath` and `rejectSharedPath` call `logAdminAction(adminId, 'shared_path.{approve|reject}', sharedPathId, {ticketId, ...})` on the `applied` branch and only on the `applied` branch. The `noop_already_terminal` branch deliberately does NOT log a second AdminAuditLog row (re-clicking an already-resolved ticket isn't a new admin action) — the audit trail counts intentions that produced state changes, not button clicks. The `conflict_state` and `not_found` branches also skip — no state change, no entry. Asserted directly in the runner tests (`expect(logAdminActionMock).toHaveBeenCalledTimes(1)` on applied; `expect(...).not.toHaveBeenCalled()` on every other branch). Combined with the `ModerationAudit{layer:5, actorId}` row this writes inside the transaction, every L5 action leaves a forensic trail in BOTH the per-path audit table (the moderation chain) AND the admin audit log (the per-admin action history), so any future "who approved this and when?" query has two independent answers that must agree.
  - **Cost gate (zero AI on L5):** structurally guaranteed by construction. L5 imports no model client — no `model-call.ts` import, no `path-generator-routing.ts` import, no `@anthropic-ai/sdk` import, no `@google/genai` import. The audit row never sets `model`, `costUsd`, `tokensIn`, `tokensOut`, `cacheReadTokens`, `cacheWriteTokens`, so every layer=5 row in `moderation_audits` will have `costUsd=0` by default. Pipeline-average cost contribution from L5: 0.
  - **Light-mode audit (per [feedback_light_mode_no_light_text](../.claude/projects/-Users-toprakdemirel-Entwicklung-Quizzard/memory/feedback_light_mode_no_light_text.md)):** verified by token resolution against `app/globals.css`. Every text color in the new `DecisionPanel` resolves dark in light mode — primary-button text via `var(--on-primary)` = `#3f0099` (deep purple, contrast 7.5:1 against the `#d1bcff` button background), danger-button text via `var(--on-error)` = `#7c0020` (deep red, high contrast against `#fea1b0` pink), ghost-button text via `var(--on-surface-variant)` = `#3a3a52` in light mode, panel body text via `var(--on-surface)` = `#0f0f1e` in light mode, helper labels via `var(--on-surface-variant)`. Zero `#fff`, zero `white`, zero light hardcoded hex values in the panel. Surface and input backgrounds reference `var(--surface-container)`, `var(--surface-container-low)`, `var(--surface-container-high)` which all auto-flip per the `[data-theme='light']` overrides at `globals.css:151-198`.
  - **Manual real-pipeline gate:** **deferred to a separate manual gate**, same shape as P4V/P5V's manual leg. The infrastructure (seeded `flagged_pending_human` fixture `seedshpsharedpathflagged1` + open ticket `seedtktsharedpathflagged1` from the P3+P4+P5 seed work) is in place; the deploy-time runbook is: (a) with real `MODERATION_L2_PROVIDER=gemini` + `MODERATION_L3_PROVIDER=anthropic` + `RESEND_API_KEY`, publish a fixture path that L1 passes, L2 flags, L3 escalates → ticket lands in the admin queue; (b) admin clicks Approve via `/admin/tickets/<id>` → assert (SQL) `SharedPath.moderationStatus='approved'`, `approvedAt` populated, `ModerationAudit{layer:5, verdict:'pass', actorId:<adminId>}` row appended, `Ticket.status='resolved'` with `resolvedById=<adminId>`, `Notification{type:'path_published'}` for the author, `AdminAuditLog{action:'shared_path.approve', adminId, targetId:<sharedPathId>}`, approval email received at the author address; (c) repeat with Reject + a reason code → assert the mirror state. Full-pipeline timing (publish → approved-after-human-click, excluding the human wait) gets recorded once a real run happens against staging. Same deferred-to-deploy shape as P4V/P5V — real provider + Resend credentials are Coolify-deployed secrets, not local dev creds.
  - **AC-Front-door (downstream gate) — public listing:** P7V calls for "path is publicly listed" after approve. P7 commits the state transition (`SharedPath.moderationStatus='approved'`, `approvedAt` set) and the supporting denormalised stats are unchanged from P1's shape, so a P8 community library reading `where: { moderationStatus: 'approved' }` will surface this path. The actual community library UI lands in P8; P7 ships the precondition (state + denormalised aggregates + cache-freshness signals) verified against the seed `approved` fixtures.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file (the two API routes + three lib files + one test under `src/__tests__`) plus the modified `app/(admin)/admin/tickets/[id]/page.tsx`; full vitest suite **291 pass + 1 skip across 19 files** (was 242 + 1 skip / 16 files at P6V — net +49 tests across +3 files from the L5 pure / runner / API suites).

  Remaining open items rolled forward:
  - The seeded-bypass branch of the publish endpoint stays silently ignored. P7 wires up the `AdminAuditLog{action:'shared_path.approve'}` action (the action enum the seeded-bypass shortcut would reuse), but the actual seeded-publish UX is P12 work (curated seed library) and not in scope here.
  - The dev seed's `flagged_pending_human` fixture keeps its hand-rolled L3 audit chain (per P5V's note). Now that L5 actions write to the same audit chain, the manual real-pipeline gate above also exercises the L5 row append against a real fan-out.
  - Layer 4 (post-publish reports + trust scoring) remains reserved for P13 per the plan's §P13.

P8 may begin.

---

### P8 — Community library UI (browse / search / filter)
**Build:**
- Public route `apps/web/app/(public)/learn/community/` (or under dashboard if auth required to browse — TBD with user; default = auth required for v1, public SEO surfaces are a v2 concern).
- Lists `SharedPath.moderationStatus = 'approved'` only.
- Filters: subject (reuse existing taxonomy), `language`, length (slot count), popularity (downloadCount).
- Search: `ILIKE` over title + description (start simple, FTS later if needed).
- API: `/api/community/paths` — pagination, filters, search — mirrors `/api/community/notebooks` patterns from `apps/web/app/api/community/notebooks/route.ts:6-125`.
- Detail page `apps/web/app/(public)/learn/community/[shareId]/page.tsx` — previews phase + checkpoint structure (titles), reserves full theory/quiz content until clone or translate.

**P8V — Verification gate:**
- Pagination correctness against 100+ fixture rows.
- Filter combinations return expected sets (matrix test: subject × language × popularity).
- Search relevance: top-K manual inspection on a known fixture corpus.
- Mobile responsiveness — library is mostly a study-side surface, so mobile matters.
- a11y baseline + light-mode audit.

**P8 status:** Shipped 2026-05-20. New endpoints:
  - `GET /api/community/paths` — auth-gated, paginated, filterable, searchable. Mirrors the envelope of `/api/community/notebooks` (`{ paths, total, page, limit, totalPages }`) with the `SharedPathListItem` shape from P0 §4.4. Query params: `page`, `limit≤50`, `search≤100`, `subject`, `language` (lowercased, BCP-47 length-capped at 8), `minSlots`, `maxSlots`, `sort=popular|recent|rating`, `filter=all|mine`. All allow-listed strings clamp to safe defaults on unknown input (no SQL-injection surface). Sort modes: `popular` → `downloadCount DESC, approvedAt DESC`; `recent` → `approvedAt DESC`; `rating` → two-queue scheme per AC-Browse-9 (above-threshold rows where `ratingCount >= MIN_RATING_SAMPLES=5` ranked by `ratingAverage DESC`, below-threshold rows tail the list in popular order) so a single 5-star vote can never outrank a 4.8/200 row. The two-queue paginator handles three boundary cases: page entirely above the boundary (one query), page entirely below (one query, with adjusted skip), and page straddling the boundary (two queries spliced).
  - `GET /api/community/paths/[shareId]` — auth-gated detail endpoint. Returns 404 for missing or non-approved rows (existence-leak guard, mirrors the admin RBAC-by-404 taxonomy). Payload is `{ source, userRating, userClonePlanId }`. The `source` snapshot includes the SharedPath metadata + a phase/slot **preview** (titles + kind + sortOrder only — no theory / flashcards / quiz bodies, per AC-Browse-6). `userRating` is the requester's own 1..5 rating (or null). `userClonePlanId` is the StudyPlan ID of an existing clone if one exists, so the UI can flip the CTA from "Clone" → "Open your copy" without a second roundtrip. `?lang=` is currently ignored — translation cache lookup is P10's surface; the detail endpoint always returns the source-language snapshot in P8. viewCount increment is fire-and-forget, rate-limited 1/(user, path)/24h via the Redis `rateLimit` helper — failure to acquire the token is the no-op branch, so a F5-spamming user can't inflate the metric. The view-counter update is wrapped in its own try/catch so a flaky `sharedPath.update` cannot 500 the detail page.

**New UI surfaces** (route group `(dashboard)/learn/community/`, auth-gated by the dashboard layout's existing redirect-to-/auth/login):
  - `app/(dashboard)/learn/community/page.tsx` — library browse surface. Header with title + a "My paths" cross-link back to `/learn/paths`. Search bar with 300ms debounce + AbortController per the existing `useSearch` shape. SegmentedControl (All / My shared). Sort dropdown (popular / recent / rating). Three filter-chip rows (Subject reusing `SUBJECT_REGISTRY` from `path-subjects.ts`, Language across the P0 popular set `[en, de, fr, es, it, tr]`, Length across the three presets short ≤9 / medium 10-18 / long ≥19). Card grid with `auto-fill, minmax(min(280px, 100%), 1fr)` so 320px phones get single-column without horizontal scroll. Each card surfaces the v1 social signals (clones / phases / rating-or-views with rating preferred when `ratingCount > 0`). Empty states distinguish between "filter=mine + no shared paths yet" and "no matches" with copy that explains *why* and points at the next action. Error state has its own retry banner. Paginator carries `aria-label`s on every direction button and dims its disabled edges via opacity 0.55 + cursor:default.
  - `app/(dashboard)/learn/community/[shareId]/page.tsx` — detail / preview surface. Back-to-library affordance, hero card (subject + language + curated-badge pill row + display-font title + author link + stat tiles for phases / checkpoints / clones / rating-or-views). CTA branches: `userClonePlanId` set → "Open your copy" Link straight to `/learn/paths/[planId]`; otherwise a deliberately-disabled "Clone to your library" button with a copy line stating cloning lands in P9 (no fake button that 404s). Path structure section renders phase cards (numbered chips + display-font heading) with slot rows showing slot title, kind chip (Learning / Review / Assessment via `SLOT_KIND_LABEL`), and a hover-state colour shift. 404 / error states surface as inline panels with their own retry / back-to-library affordances, never as a route-level error boundary.

**Modified files:**
  - `app/(dashboard)/learn/paths/page.tsx` — added a "Browse community" Link next to the existing "New path" button. Visually de-emphasised (surface-container background + outline-variant border) so the primary "New path" CTA still leads, but discoverable from the user's-own-paths surface where they're most likely to think "what else is out there?". Matches the `project_paths_led_growth` memory's stance that paths are the front door for the funnel.
  - `prisma/seed-path-publishing.ts` — extended with 16 approved library fixtures spanning 4 languages × 4 subjects × 3 slot buckets. Cycles ownership across the existing 3 seed users so `filter=mine` lands on non-empty results across all three logins; approve timestamps step backwards by `idx` days so `sort=recent` is deterministic across reruns. Stable cuid-shaped IDs (`seedshplibrary<NN>shp`) keep the upserts idempotent. The P8V "100+ fixture rows" gate is verified numerically in code (see `paths-list.test.ts § pages cleanly across a 100+ row fixture set` — 117-row mocked pagination); the 16-row staging set is for the manual filter-matrix smoke test, not a stress fixture.

**Hallmark pre-emit critique on the new surfaces:** `P5 H4 E4 S5 R5 V4` — all six axes ≥ 4, no revision pass. Both pages defer to the project's locked design system (token-driven inline styles per `figma-design-system.md`, Material Symbols Outlined icons, no Tailwind utilities except the listed customs, no `transition-all`, no gradients per `feedback_no_gradients`). Macrostructure variety vs. the P7 DecisionPanel: P7 was an asymmetric two-button decision panel; P8 is a filter-strip + searchable card grid for the list and a hero + numbered phase tree for the detail — different rhythm, different reveal cadence, both grounded in the same token palette. The disabled clone CTA + the inline "lands in P9" rationale is the honesty axis — no fake buttons, no fabricated "coming soon" toasts.

**P8V signed off 2026-05-20:**
  - **Pagination correctness against 100+ fixture rows (P8V leg 1):** `paths-list.test.ts § pages cleanly across a 100+ row fixture set` asserts page 5 of a 117-row result returns 20 paths, `skip=80`, `total=117`, `totalPages=ceil(117/20)=6`. The route's `skip = (page-1) * limit` is also asserted independently. Page clamp tests cover `page=-99` → page 1, `limit=9999` → clamped to 50, and `page` defaulting to 1 with `limit` defaulting to 20.
  - **Filter combinations return expected sets (P8V leg 2, matrix test):** `paths-list.test.ts § filter composition` asserts the where-clause shape for every combination: subject alone → `subjects: { has }`; language alone → lowercased + length-capped (longer-than-8 → undefined); minSlots + maxSlots → `slotCount: { gte, lte }`; minSlots alone → `slotCount: { gte }`; subject × language × minSlots × maxSlots × search composing together → single `where` object with all five constraints + the approved gate retained. `filter=mine` adds `sharedById = userId` and **keeps** `moderationStatus = 'approved'` — no path leaks pending/rejected rows via the mine filter. Unknown filter / sort / status values clamp to safe defaults (the "no SQL injection surface" guard).
  - **AC-Browse-9 invariant — single 5-star can't outrank 4.8/200:** `paths-list.test.ts § AC-Browse-9 invariant` plants two fixtures (one above-threshold `4.8/200`, one below-threshold `5.0/1`) and asserts the above-threshold row comes first. The rating-sort two-queue logic is also exercised across all three boundary cases (page entirely above, page entirely below, page straddling the boundary).
  - **Search relevance + length cap (AC-Browse-3):** `paths-list.test.ts § search` asserts the search applies `ILIKE`-equivalent (`contains` + `mode:'insensitive'`) over **both** `title` AND `description`, caps input at 100 chars (no runaway input → DOS surface), and treats whitespace-only input as no-filter. Top-K manual relevance inspection is deferred to the staging smoke pass (same shape as the P4V/P5V/P6V/P7V manual legs); the 16-fixture library seed gives staging enough title/description variety to exercise it.
  - **Detail endpoint (`paths-detail.test.ts`, 14 tests):** 401 on unauthenticated; 404 on missing row; 404 on every non-approved status (`pending` / `auditing_l2` / `auditing_l3` / `flagged_pending_human` / `rejected` — covered via `it.each`); happy path returns the source snapshot + phase/slot preview + `userRating`; phases load `orderBy: { sortOrder: 'asc' }` and slots ditto; the slot select is the narrow preview shape (`{ id, title, description, kind, sortOrder }`) and explicitly NOT the activities / theory / flashcardSet / quizSet relations (AC-Browse-6 regression sentinel — if a future change accidentally pulls theory bodies into the list/detail payload, this test fails). `userClonePlanId` flips from null to a planId when the requester has an existing clone. The view-counter throttle: increment fires when `rateLimit({ success: true })`, does NOT fire on `{ success: false }`, the key is `viewer-1` + the shareId, and a `sharedPath.update` failure does NOT surface to the user (the detail page still 200s).
  - **AC-Browse-5 auth + existence-leak guard:** the list endpoint returns 401 without touching the DB on unauth; the detail endpoint returns 404 (not 401) when the row exists but isn't approved — same existence-leak policy as the admin RBAC-by-404 taxonomy. Both 401 and 404 paths have explicit `not.toHaveBeenCalled()` assertions on every downstream Prisma query so a future regression that "leaks via timing" trips.
  - **AC-Browse-8 social-signals payload shape:** asserted directly in the list-endpoint payload-shape test — every list item exposes `{ shareId, title, description, coverImageUrl, language, subjects, phaseCount, slotCount, downloadCount, viewCount, ratingAverage, ratingCount, seeded, approvedAt, author }`. The `select` clause is checked to NOT include `plan` / `audits` / `translations` so the list never bloats with theory/audit data.
  - **Mobile responsiveness (P8V leg 4):** both pages use `gridTemplateColumns: 'repeat(auto-fill, minmax(min(280px, 100%), 1fr))'` / `auto-fit, minmax(min(140px, 100%), 1fr)` — the `min(N, 100%)` clamp guarantees no horizontal scroll at 320px (a bare `1fr` minimum would have overflowed). Filter chip strips wrap via `flexWrap: 'wrap'`. Hero h1 uses `overflowWrap: 'anywhere'` to handle long titles without breaking the layout. Slot title rows use `flex:1 + overflow:hidden + textOverflow:ellipsis` for graceful long-title truncation. No `width: 100vw` declarations anywhere — the dashboard layout's `overflow-x: hidden` handles the outer container, and every inner grid is `minmax(0, 1fr)`-safe (Hallmark gate 61 satisfied).
  - **a11y baseline (P8V leg 5):** every Material Symbols icon is `aria-hidden`; search input carries `aria-label="Search community paths"`; sort `<select>` carries `aria-label="Sort by"`; SegmentedControl uses `role="tablist"` + `role="tab"` + `aria-selected`; FilterChipRow uses `role="group"` + `aria-label` + per-chip `aria-pressed`; ResultsMeta uses `aria-live="polite"`; phase list is semantic `<ol>`; slot list is semantic `<ol>`; stat tiles use `<dl><dt><dd>` (titles read as definition-pairs); back-buttons + paginator buttons have explicit `aria-label`s; disabled clone button has `aria-disabled="true"`; error panels use `role="alert"`. No element sets `outline: none`, so the project's globals.css `:focus-visible` defaults flow through; the four key interactive classes (`.community-path-card`, `.community-filter-chip`, `.community-paginator-btn`, `.community-detail-cta`) carry explicit `:focus-visible` rings at 2–3px with the primary token for ≥3:1 contrast against any surface in the design system.
  - **Light-mode audit (per [feedback_light_mode_no_light_text](../.claude/projects/-Users-toprakdemirel-Entwicklung-Quizzard/memory/feedback_light_mode_no_light_text.md)):** the four new files were grepped for hardcoded hex / `white` / `#fff` colour strings — zero hits. Every text colour resolves via `var(--on-surface)`, `var(--on-surface-variant)`, `var(--on-primary)`, `var(--on-error)`, or `var(--primary)` (used as a brand accent — same pattern as the established `/learn/paths` SubjectChip at `apps/web/app/(dashboard)/learn/paths/page.tsx:795-810`). Every surface uses `var(--surface-container*)`, `var(--surface)`, or the project's tokenised RGBA accent overlays — all auto-flip per the `[data-theme='light']` overrides at `globals.css:151-221`.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file (two API routes + two pages + two test files + the seed extension) plus the modified `app/(dashboard)/learn/paths/page.tsx` (the pre-existing `react-hooks/set-state-in-effect` baseline at line 145 is the same one P2V flagged as not-introduced-by-this-phase — `git diff` confirms this phase's change is the header block at lines 252-313, far from the effect). Full vitest suite **339 pass + 1 skip across 21 files** (was 291 + 1 skip / 19 files at P7V — net +48 tests across +2 new test files from the community list + detail suites).

  Remaining open items rolled forward:
  - **Top-K search-relevance manual inspection:** deferred to the staging smoke pass per the same deferred-to-deploy shape as the P4V/P5V/P6V/P7V manual legs. The 16-fixture library seed gives staging enough variety to walk through `subject × language × length × sort` + a handful of representative `search` strings ("python", "spanish", "biology", "math") and verify the top-K results match intuition.
  - **Clone CTA wiring:** P8 ships the affordance as a deliberately-disabled button + a `userClonePlanId`-aware "Open your copy" link. `POST /api/community/paths/[shareId]/clone` lands in P9 along with the `clonedFromSharedPathId` deep-copy.
  - **Translation cache lookup:** the detail endpoint silently ignores `?lang=` and always returns the source snapshot. The cache + on-demand translation flow lands in P10 along with the per-language daily budget guard and the single-flight insert-on-conflict lock.
  - **Rating editor:** the detail endpoint exposes `userRating` so a future rating editor can pre-populate. The `POST` / `DELETE /api/community/paths/[shareId]/rating` endpoints + the editor UI are a post-P8 deliverable (P10/P11-adjacent).

P9 may begin.

---

### P9 — Path cloning (fork-on-clone)
**Build:**
- `POST /api/community/paths/[shareId]/clone` — creates a private `StudyPlan` for the requester, deep-copying phases + checkpoint slots + activities + theory/flashcards/quiz content.
- Tracks `clonedFromSharedPathId` on the new StudyPlan (new column added in P1).
- Increments `SharedPath.downloadCount`.
- Idempotent: if requester already has a clone of this `SharedPath`, returns existing clone (don't duplicate).
- Does NOT copy progress / streak / completion state — fresh start.

**P9V — Verification gate:**
- Unit: clone preserves structure but assigns fresh IDs; existing IDs unchanged.
- Edits to the original (republished) do NOT affect existing clones (fork model verified).
- Manual: free user clones a public path and starts using it — validates the value prop end-to-end.
- Idempotency: repeated clone requests return the same plan ID.
- Cost: pure DB operation, no AI — cost gate trivially met.

**P9 status:** Shipped 2026-05-20. New endpoint:
  - `POST /api/community/paths/[shareId]/clone` — auth-gated, approved-only (404 on missing / non-approved per AC-Clone-1), idempotent per `(userId, sharedPathId)` (AC-Clone-5). Cheap pre-checks outside the transaction (existence + approved gate + existing-clone lookup) avoid holding a DB connection on the common 404 / already-cloned paths; the interactive transaction inside re-validates everything as the source of truth.
  - Deep-copy lives inside a single `db.$transaction(async (tx) => …)` with `timeout: 30_000`. Default Prisma timeout (5s) would trip on the upper end of slot × activity × flashcard graphs; we're nowhere near connection starvation territory at v1 traffic so the longer ceiling is fine. The transaction wraps three writes: (a) the in-tx race re-check on `(userId, clonedFromSharedPathId)` so a concurrent self-clone collapses to the racer-winner planId + `alreadyCloned:true` (zero create, zero increment), (b) the nested-create on StudyPlan that allocates fresh cuids and threads FKs through every relation in one round-trip, (c) the atomic `SharedPath.downloadCount` increment per AC-Clone-6 — keeping the +1 inside the same tx is what stops a rolled-back clone from leaking a phantom increment.

**Deep-copy graph (AC-Clone-2 + AC-Clone-3 + AC-Clone-4):**
  - `StudyPlan` — fresh `userId=cloner`, fresh `startDate=now`, `endDate=now+30d` (mirrors the seed and manual-path defaults), `source='manual'`, `generationStatus='ready'`, `generationProgress=null`, `generationError=null`, `language` + `subjects` snapshotted from the **SharedPath** (not the source plan — the SharedPath snapshot is the canonical surface the cloner picked from), `subjectWeights` reset to 1-per-subject, `clonedFromSharedPathId=shareId` (AC-Clone-4 analytics anchor).
  - `StudyPhase` — `status='upcoming'`, fresh `startDate`/`endDate` mirroring the plan's window, original `title` / `description` / `sortOrder` / `gateStrategy` carried through.
  - `CheckpointSlot` — `starsEarned=0`, `bestPercentage=null` (AC-Clone-3 progress reset), `prerequisiteSlotIds=[]` (the source's prereq IDs reference source-plan slot IDs that don't exist on the clone — the schema's sequential gateStrategy default still walks the path correctly, so v1 just clears the per-slot prereq overrides).
  - `CheckpointActivity` — `completed=false`, `completedAt=null`, kind-specific content branch (theory / flashcardSet / quizSet) populated via Prisma's nested-create.
  - `TheoryContent` — TipTap JSON `body` carried through verbatim (no per-user state on this row).
  - `FlashcardSet` — `userId=cloner`, `source='manual'` (cloned content surfaces as user-curated regardless of the original "ai" source — keeps the cloner's AI-quota and cohort filters honest), `notebookId`/`chatId`/`messageId`/`sectionId` all null (the clone exists in /learn standalone, not inside a notebook).
  - `Flashcard` — SM-2 SR fields reset to defaults (`easeFactor=2.5`, `interval=0`, `repetitions=0`, `nextReviewAt=null`, `lastReviewAt=null`) so the cloner's review schedule isn't pre-warped by the original author's review history (AC-Clone-3).
  - `FlashcardImage` — metadata copy with `filePath` carried by reference (the blob in Supabase storage is persistent and NOT ref-counted; the cloned card continues to render even if the original author later deletes their FlashcardSet, since the cascade nukes rows but not blobs).
  - `QuizSet` — `userId=cloner`, all notebook/chat/section pointers cleared.
  - `QuizQuestion` — `kind` (the full `QuestionKind` enum, not a narrow subset — `true_false` / `fill_blank` / `word_bank` / `match_pairs` / `sentence_reorder` / `equation` / `translation` / `code_output` / `timeline` / `code_write` all carry through), `payload` JSON copied as-is, legacy MC fields (`options`, `correctIndex`) preserved verbatim until the Phase-7 path-generator backfill ports them.

**Modified files:**
  - `app/(dashboard)/learn/community/[shareId]/page.tsx` — the P8 disabled clone button is replaced with a real `CloneCTA` that POSTs to the new endpoint and pushes the user to `/learn/paths/[planId]` on success. Loading state ships the full 8-state interactive contract (default · hover · `:focus-visible` 3px outline · `:active` translateY(1px) · disabled via `aria-busy`+`aria-disabled` · loading via spinning `progress_activity` icon + label flip to "Cloning…" · error via `role="alert"` panel under the rationale text · success via `router.push`). The spinner animation is keyframe-driven on `transform: rotate(0deg → 360deg)` only — no `transition-all`, no layout-property animation. `prefers-reduced-motion: reduce` disables both the spinner and the slot hover transition. 404 / 409 surface as inline error copy under the CTA; the existing detail-page error boundary still owns the "the path itself failed to load" case. Re-visiting after a successful clone (page reload) keeps the existing `userClonePlanId`-aware "Open your copy" Link branch from P8 — that path was already wired correctly and required no changes.

**Hallmark pre-emit critique on the new surface:** `P5 H4 E5 S4 R5 V4` — all six axes ≥ 4, no revision pass. The CloneCTA defers to the project's locked design system (token-driven inline styles per `figma-design-system.md`, Material Symbols Outlined icons, no Tailwind utilities except the listed customs, no `transition-all`, no gradients per `feedback_no_gradients`). Macrostructure variety vs. P8's hero + filter grid: P9's contribution is a state-machine micro-component, not a macrostructure shift — the panel inherits P8's hero rhythm and only the CTA mutates between states. The label flip + icon swap (download → progress_activity) over a single button avoids the templated "loading overlay across the whole card" pattern; the inline `role="alert"` error tucks under the rationale text rather than firing a global toast (silent success / inline failure beats celebratory toast per the microinteractions stance).

**P9V signed off 2026-05-20:**
  - **Unit (clone preserves structure, fresh IDs, source forensic — AC-Clone-2 + AC-Clone-4 + AC-Clone-7):** `paths-clone.test.ts § first clone § writes a deep-copy whose StudyPlan carries clonedFromSharedPathId + cloner userId` asserts the create payload's StudyPlan-level fields verbatim: `userId='user-1'`, `clonedFromSharedPathId='shp-1'`, `language='en'`, `subjects=['coding','general']`, `source='manual'`, `generationStatus='ready'`. The "fresh IDs" invariant is structurally guaranteed by Prisma's nested-create — every node in the graph (StudyPlan, StudyPhase, CheckpointSlot, CheckpointActivity, TheoryContent, FlashcardSet, Flashcard, FlashcardImage, QuizSet, QuizQuestion) gets a brand-new cuid allocated at insert time because the data payload never carries an `id` field. The companion `§ deep-copies phases + slots + activities with the source content` test asserts the full content graph: 1 phase → 3 slots → 1-activity-each → kind-specific content (theory body / flashcards-with-image / quiz with MC options), every kind discriminator carries through, every cross-plan FK reference (source-plan prereq slot IDs) is cleared on the clone, every slot's `prerequisiteSlotIds=[]`.
  - **AC-Clone-3 — progress reset everywhere:** asserted at four levels in the same § deep-copies test. StudyPlan: `generationStatus='ready'`, `generationProgress=null` (set via `Prisma.JsonNull` per the schema's `Json?` declaration). CheckpointSlot: `starsEarned=0`, `bestPercentage=null` for all three slots. CheckpointActivity: `completed=false`, `completedAt=null` on all three activity kinds. Flashcard SR state: `easeFactor=2.5`, `interval=0`, `repetitions=0`, `nextReviewAt=null`, `lastReviewAt=null` on every cloned card.
  - **AC-Clone-5 — idempotency (no re-clone, no re-increment):** `§ idempotency` asserts that when the requester already has a clone for `(userId, shareId)`, the route returns 200 with `{ planId: 'plan-existing', alreadyCloned: true }`, and crucially **does not** open a transaction (`$transaction` not called) or update `downloadCount` (`sharedPath.update` not called). A naive implementation that always incremented would fail this assertion.
  - **AC-Clone-6 — downloadCount increment lives inside the transaction:** `§ opens a single transaction wrapping the create + downloadCount increment` asserts `$transaction` was called exactly once and `sharedPath.update` was NOT called outside the tx; the companion `§ increments downloadCount inside the transaction with +1` captures the tx-scoped update call and asserts the exact `{ where: { id: 'shp-1' }, data: { downloadCount: { increment: 1 } } }` shape. A rolled-back transaction can't leak a phantom +1 by construction because the increment is co-located with the StudyPlan insert.
  - **In-tx idempotency race (concurrent self-clone):** `§ returns the racer-winner planId + alreadyCloned=true when the in-tx re-check finds one` — when the in-tx `findFirst` returns a winner (a concurrent clone landed between the pre-check and the start of the tx), the runner returns that winner's planId + `alreadyCloned:true`, and explicitly does NOT call `studyPlan.create` or `sharedPath.update`. The +1 increment is suppressed on the racer-second so the counter cannot double-count from a single user (AC-Clone-6 contrapositive).
  - **AC-Clone-1 + existence-leak guard:** `§ auth` asserts 401 on missing session with zero DB touch; the parametrised `it.each([pending, auditing_l2, auditing_l3, flagged_pending_human, rejected])` covers every non-approved status against a 404 response shape (existence-leak-policy parity with the rest of the path-publishing surface — no 403 / 401 timing-distinguishable cases). The defensive between-checks unpublish path (`§ returns 404 if the path is unpublished between pre-check and snapshot load`) covers the mid-flight race where the source SharedPath disappears between the route's pre-check and the deep-load snapshot — same 404 response, no transaction opened.
  - **AC-Clone-7 — edits to the original don't flow into existing clones (fork model):** structurally guaranteed by the deep-copy. The cloned StudyPlan stores forensic values (`title`, `description`, `language`, `subjects` from the SharedPath at clone time), not references back to the source plan or SharedPath. The only persisted cross-reference is `clonedFromSharedPathId` (analytics anchor, `onDelete: SetNull` so an unpublish doesn't cascade-delete the clone). A future `republish` on the source would update the SharedPath snapshot; existing clones would still serve the version-at-clone-time content because their rows are independent copies.
  - **Cost gate (P9V "pure DB operation, no AI" — trivially met):** structurally guaranteed by construction. The route imports zero model clients (`@/lib/moderation/model-call.ts`, `@/lib/path-generator-routing.ts`, `@anthropic-ai/sdk`, `@google/genai` — none referenced); the test suite asserts zero model-call mocks would fire because none are stubbed. Pipeline-average cost contribution from P9: 0.
  - **Manual real-pipeline gate:** **deferred to a separate manual gate**, same shape as P4V/P5V/P6V/P7V/P8V's manual leg. The infrastructure (seeded approved fixtures `seedshpsharedpathapproved` + the 16-row `seedshplibrary*` set from P8) is in place; the deploy-time runbook is: (a) sign in as a free user (`seedusrsharedpathauthor0002` / FREE tier); (b) navigate to `/learn/community/seedshpsharedpathapproved`; (c) click "Clone to your library"; (d) assert the redirect lands on `/learn/paths/<new-planId>`, the new plan is visible in `/learn/paths`, `SharedPath.downloadCount` incremented by 1 in SQL, the cloned plan has zero `AssessmentAttempt`/`QuizAttempt` rows, `starsEarned=0` on every slot, `clonedFromSharedPathId` populated; (e) repeat the clone — assert the second click is a no-op idempotent retry returning the same planId with `alreadyCloned:true`, downloadCount stays at the post-first-clone value. The dev seed's library fixtures don't yet plant nested phase / slot / activity content, so the full content-shaped E2E walkthrough happens at deploy-time against a path that did go through the full publish flow (or against a seed-extended fixture, future work). The P0 §4.6 "popularity-threshold pre-translation fan-out" post-commit hook is explicitly deferred to P11 — that phase owns both the env gate (`POPULAR_LANGUAGES`, `POPULARITY_THRESHOLD`) and the fan-out itself; backfill for paths that crossed the threshold pre-P11 is covered by P11's admin manual-trigger endpoint.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file (the new route + the new test file) plus the modified `app/(dashboard)/learn/community/[shareId]/page.tsx`; full vitest suite **356 pass + 1 skip across 22 files** (was 339 + 1 skip / 21 files at P8V — net +17 tests across +1 new test file from the clone integration suite).

  Remaining open items rolled forward:
  - **Manual content-shaped E2E walkthrough:** deferred to the deploy-time runbook above. Same deferred-to-deploy shape as P4V/P5V/P6V/P7V/P8V — the unit-test suite verifies the contract against mocked DB boundaries; the runbook exercises it against a real Postgres + the full Supabase storage backend for `FlashcardImage.filePath` references.
  - **Popularity-threshold pre-translation fan-out:** the P0 §4.6 post-commit hook (atomic `popularityTriggeredAt` flip + fire-and-forget P11 fan-out) is deferred to P11 along with the env gate and the actual fan-out itself. P9 ships the canonical clone surface; P11 will wire the popularity-threshold hook on top of it without modifying the clone graph.
  - **`generationProgress` reset via `Prisma.JsonNull`:** the `Json?` column doesn't accept JS `null` in a create-data payload (Prisma's type system distinguishes "DB NULL" from "JSON null"); we use `Prisma.JsonNull` to mean "store DB NULL" so subsequent updates that read it back as JS `null` work correctly. Same pattern used elsewhere in the path-generator for the same reason.
  - **Cloned-from-empty-source defensive fallback:** when a CheckpointActivity has neither theory / flashcardSet / quizSet (a malformed source row), the deep-copy emits an empty theory-shell so the slot is at least clickable on the clone rather than crashing the whole transaction. Logged via `console.warn` for triage; expected zero hits in practice but the guard keeps the clone resilient against unforeseen data shapes.

P10 may begin.

---

### P10 — Translation cache + on-demand translation
**Build:**
- `GET /api/community/paths/[shareId]?lang=de` — if `PathTranslation` cached for `(shareId, 'de')`, serve from cache; else translate inline (synchronous on first request, bounded ~30s) and write to cache.
- Translation prompt with `buildCachedSystem()` for the system / instruction block so it cache-hits.
- **Single-flight:** unique constraint on `(sharedPathId, targetLanguage)` + `INSERT … ON CONFLICT DO NOTHING` → wait → re-read. Prevents 2 concurrent users triggering 2 AI calls.
- **Rate limit** per user + per IP on translation triggers (anti-abuse cap).
- **Per-language daily budget guard** — if today's translation spend in language `L` exceeds the budget, refuse new on-demand translations into `L` with a "try later" UX. Pre-baked popular languages are exempt because they're free at view time.
- Source of truth for translation: copy the SharedPath snapshot into the translation row at creation time so future edits to original (rare; would require re-publish flow) don't desync cached translations.

**P10V — Verification gate:**
- Unit: first request → AI call + cache write; second request → cache hit, zero AI call.
- Concurrency: 5 parallel requests for the same `(path, lang)` → exactly 1 AI call (verified via mock provider call counter).
- Manual: translate a known German path to English; verify content is readable English; verify second request is instant.
- **Cost gate:** mean per-translation cost recorded; ≤ P0 budget.
- Rate limit: 6th request from same user/IP within window → 429.
- Daily-budget guard: simulated over-budget condition rejects gracefully.

**P10 status:** Shipped 2026-05-21. New modules:
  - `apps/web/src/lib/translation/prompt.ts` — pure module owning the byte-identical `TRANSLATION_RUBRIC` (cache-stability invariant — no language pair baked in so the prompt-cache key is stable across every `(path, lang)` combination), the `T_ANTHROPIC_TOOL` + `T_GEMINI_SCHEMA` structured-output contract (lockstep mirror — schema drift would land in both edits at once), `buildTranslationPayload()` (per-call tail; plain-text labelled blocks beat JSON-in-JSON for both token efficiency and parser-ambiguity), `parseTranslationResponse()` (defensive post-hoc validation — both providers already constrain via tool / responseSchema, but a fail-closed parse on retry surfaces as `status='failed'` rather than corrupting the cache with a malformed payload), and `projectTranslationOnto()` (re-keys the model output by source IDs so an LLM that scrambles slot order doesn't scramble the persisted row — dropped IDs fall through to source titles rather than crashing the whole translation).
  - `apps/web/src/lib/translation/provider.ts` — `TRANSLATION_PROVIDER` env-var dispatcher with default `gemini`. Anthropic branch routes Haiku (cheap, matches the P0 §7.4 per-translation target ≤ $0.10), Gemini branch routes 2.5 Flash. Rubric is the cached leading block on both providers (`cache_control: ephemeral` on Anthropic, leading concat on Gemini for implicit caching — same pattern as `moderation/model-call.ts`).
  - `apps/web/src/lib/translation/snapshot.ts` — `loadTranslatableSnapshot(shareId, lang)`. Returns the structural overlay (title + description + phase + slot titles + descriptions) the detail page renders per AC-Browse-6. Theory / flashcards / quiz content is deliberately out of v1 scope — translating them would 3-5× the per-translation cost without changing what users see on the detail page; the schema's `payload Json?` column allows for an additive expansion in a later phase.
  - `apps/web/src/lib/translation/runner.ts` — `runTranslation(shareId, snapshot)` orchestrates the provider call, accumulates per-attempt usage in a meter, computes the final USD cost via `computeCost()` from `path-generator-cost.ts` (same rate card as moderation), and atomically updates the existing `PathTranslation` row to `ready` / `failed`. The route owns the row's existence (single-flight insert lives there), the runner owns the state transition. Fail-closed: any throw inside the model call or parser surfaces as `status='failed'` with the failure reason — quota stays untouched (caller's increment site is post-success).

**Modified files:**
  - `apps/web/app/api/community/paths/[shareId]/route.ts` — `?lang=` parser + the full gate stack from P0 §4.5. Order of checks: (a) cache lookup — `ready`/`translating`/`failed` short-circuit without burning rate-limit / quota / budget tokens; (b) per-user (6 / 10 min) + per-IP (12 / 10 min) rate limit; (c) `checkUsageLimit(userId, 'path_translation')` — FREE lifetime exhaustion returns **402** with `upgrade: true`, PRO monthly cap returns **429** with support copy; (d) per-language daily budget (`TRANSLATION_DAILY_BUDGET_<L>_USD`, $5/day default) — 429 with "try later" when exceeded; (e) single-flight claim via `db.pathTranslation.create` (P2002 path on race-lose → re-read existing row) OR `updateMany` with status guard for failed-row retries; (f) on AI-call success, `incrementUsage(userId, 'path_translation')` — failed AI calls do NOT increment. Response shape extends from `{ source, userRating, userClonePlanId }` to `{ shareId, language, source, translation?, userRating, userClonePlanId }` — `language` is the language the response was served in (source for source-lang / `translating` / `failed`; the requested lang for `ready` cache hits), `translation` is the typed envelope discriminated on `status`. Source-language requests (no `lang` or `lang === source`) short-circuit the entire translation branch — no DB writes, no rate-limit ticks, no quota check — so re-visits of a path in its own language never burn anti-abuse tokens.
  - `apps/web/app/(dashboard)/learn/community/[shareId]/page.tsx` — Hallmark-stamped `LanguagePicker` chip rail (component-scope `P5 H4 E5 S4 R5 V4` — all six axes ≥ 4, no revision pass; macrostructure skipped per component-scope rules, theme inherited from the project's locked NoteMage tokens, mobile-responsive flex-wrap). Pops the templated `<select>` dropdown by going with a horizontal chip rail; the 8-state contract (default · hover · `:focus-visible` 3px primary outline + 2px offset · `:active` translateY(1px) · disabled via `disabled` attr · loading via `aria-busy` + `progress_activity` icon swap + cursor=progress · error via inline `role="alert"` panel · success via `aria-pressed=true` + primary fill on the active chip) is documented inline as a CSS comment per Hallmark's component-stamp format. Polling for `status='translating'` runs in the parent component with a `requestEpochRef` guard so stale polls can't write to state after the user switches language mid-flight. 402 / 429 / `failed` envelope each surface a distinct inline UX — 402 shows an "Upgrade to Pro" link, 429 / `failed` show a retryable inline alert without flooding a toast. `prefers-reduced-motion: reduce` disables both the chip spinner and the slot-hover transition; the spinner is keyframe-driven on `transform: rotate(0deg → 360deg)` only — no `transition-all`, no layout-property animation, no gradients per `feedback_no_gradients`.

**P10V signed off 2026-05-21:**
  - **Unit (AC-Translate-1 source-lang short-circuit):** `paths-translate.test.ts § source language (AC-Translate-1)` covers four branches: 401 on unauth (zero DB touch on the translation surface), no-`?lang=` returns `{ translation: null }` with zero `pathTranslation.findUnique` / `runTranslation` / `checkUsageLimit` calls, `?lang === source.language` short-circuits with the same zero-touch guarantees, and a malformed `lang` query (`'not_a_bcp47!!'`) returns 400 with zero DB touch. The `LANG_PATTERN` regex (`/^[a-z]{2,3}(-[a-z0-9]{2,4})?$/`) accepts the popular set plus `pt-br` / `zh-tw` style sub-tags. The existence-leak guard is asserted in the same describe block — a `pending` SharedPath returns 404 with zero `runTranslation` calls (no timing distinguishability vs missing row).
  - **AC-Translate-2 (first request → AI + cache write + quota):** `§ first request (AC-Translate-2) § fires the runner exactly once` mocks the cache lookup to null, then asserts (a) `db.pathTranslation.create` was called with `{ data: { sharedPathId, language, status: 'translating' } }` (single-flight claim before any AI tokens are spent); (b) `runTranslation` was called exactly once with the loaded snapshot (forensic copy — `runTranslation('shp-1', expect.objectContaining({ sourceLanguage: 'de', targetLanguage: 'en', title: 'Source title' }))`); (c) `incrementUsage('user-1', 'path_translation')` fired exactly once after the runner returned a `ready` result; (d) the response envelope is `{ status: 'ready', language: 'en', payload, cachedAt }` with the re-read `updatedAt` as the canonical timestamp.
  - **AC-Translate-3 (cache hit → zero AI call):** `§ cache hit (AC-Translate-3) § returns the cached payload without calling the runner` asserts the contrapositive — when `pathTranslation.findUnique` returns `{ status: 'ready', payload, updatedAt }`, the route returns the cached payload AND `runTranslation` was never called AND `checkUsageLimit` was never called AND `incrementUsage` was never called AND `pathTranslation.aggregate` (daily-budget) was never queried AND `pathTranslation.create` was never called. The cache hit bypasses every gate by design — the originating user already paid those costs on the first cache miss.
  - **AC-Translate-4 (single-flight — exactly 1 AI call):** `§ single-flight (AC-Translate-4) § the race-loser sees P2002, re-reads, and returns the winner's row WITHOUT firing the runner` constructs a real `Prisma.PrismaClientKnownRequestError({ code: 'P2002' })` and throws it from the `create` mock to simulate the unique-constraint loss. The route catches the P2002, re-reads the row (which returns the winner's `translating` state), and returns the `translating` envelope. `runTranslation` was called zero times by the loser (the winner runs it in a separate request the test doesn't simulate, but the loser path is what AC-Translate-4 actually mandates — the racer-second MUST NOT fire a second AI call). The loser also does not call `incrementUsage` — the winner's quota burn is sufficient for the (path, lang) pair.
  - **AC-Translate-5 (translating-status polling):** `§ translating status (AC-Translate-5) § returns translating envelope without calling the runner or burning quota` mocks the cache lookup to return `{ status: 'translating' }` and asserts the response carries `{ status: 'translating', language: 'en' }` with zero runner / quota / budget / create-row touches. The client picks up the polling cadence (2s) from the page's `pollTimerRef` re-fetch loop in `[shareId]/page.tsx`.
  - **AC-Translate-7 (per-language daily budget):** two tests cover the budget guard. The default-cap test mocks today's spend in `en` at $7.50 (over the $5.00 default ceiling) — route returns 429 with zero `runTranslation` / `pathTranslation.create` / `incrementUsage` calls. The env-override test sets `TRANSLATION_DAILY_BUDGET_EN_USD=1.0` and asserts the env knob takes precedence: $1.50 spend → 429 even though it's well under the default $5 cap. The env-var read happens at request time (not module-load), so Coolify env rotations land without a redeploy.
  - **AC-Translate-8 (rate limits — user + IP):** two tests cover the rate-limit branch. The user-limit test mocks `rateLimit` to succeed for the viewCount fire-and-forget call but fail with `{ success: false, retryAfterMs: 30_000 }` on the per-user translation call — route returns 429 with zero runner calls. The IP-limit test mocks user-limit success then IP-limit fail — same 429, zero runner calls. The check order (user-then-IP) is structurally encoded in the route so a single test pair is sufficient to lock the behaviour.
  - **AC-Translate-10/11 (FREE lifetime exhaustion → 402 + upgrade body):** `§ quota gates § FREE lifetime exhausted` mocks `db.user.findUnique` → `{ tier: 'FREE' }` and `checkUsageLimit` → `{ allowed: false, used: 5, limit: 5 }`. The route returns **402** with `{ success: false, error, upgrade: true }`. `runTranslation` and `incrementUsage` are both never called — the gate fires BEFORE the AI call AND BEFORE the daily-budget check (per P0 §4.5 the personal cap is the user-visible gate; the daily budget is the server-side safety net on top, so personal-cap messages take precedence). The FREE-within-quota counterpart test (`§ FREE user within quota → request proceeds to runner`) asserts the inverse — `allowed: true, used: 3, limit: 5` lets the request flow through and the runner + increment both fire.
  - **AC-Translate-12 (PRO monthly cap → 429):** `§ PRO monthly cap → 429` mocks `db.user.findUnique` → `{ tier: 'PRO' }` and `checkUsageLimit` → `{ allowed: false, used: 50, limit: 50 }`. The route returns **429** (anti-abuse safety net, not an upgrade prompt) with zero runner / increment calls. The two status codes (402 for FREE, 429 for PRO) are intentionally distinct so the client renders the right copy without parsing the message body.
  - **AC-Translate-9 + failed-row retry semantics:** two tests cover the retry path. `§ claims the failed row via updateMany + runs the translation again` mocks the existing row at `status='failed'`, `updateMany` matching 1 row (we won the retry lock), and asserts the route runs the translation + increments quota. The Prisma `updateMany.where` is asserted as `{ sharedPathId, language, status: 'failed' }` — the status guard is what makes the retry race-safe (a concurrent retryer that beats us flips status to `translating` first, so our `updateMany` matches zero rows). The race-loser test (`§ returns the racer-winner's status when updateMany matches zero rows`) mocks `count: 0` on the updateMany and a `ready` row on the re-read — route returns the winner's payload with zero `runTranslation` / `incrementUsage` calls.
  - **AC-Translate-9 (snapshot forensics):** `§ snapshot semantics (AC-Translate-9) § hands the runner a forensic snapshot` asserts the runner receives the snapshot object loaded at translation time — a later mutation on the source SharedPath cannot retroactively flow into the cached row because the runner's argument is a forensic copy, not a live reference. Structurally guaranteed by `loadTranslatableSnapshot` returning a plain shape (no Prisma lazy relations); the test pins the contract against the mock.
  - **Runner failure surface:** `§ runner failure § surfaces a failed envelope and does NOT increment quota` mocks `runTranslation` returning `{ status: 'failed', error: 'Provider unavailable' }`. Route returns 200 with `{ translation: { status: 'failed', language, error } }` and `incrementUsage` is never called. The 200 (not 5xx) is deliberate — the source snapshot is still readable, the translation failure is an inline UX concern not a server error. The `failed` envelope flips the page to its retry path (`translationError` with `retry: true`).
  - **Pure-module unit gate (`translation-prompt.test.ts`, 14 tests):** rubric stability (`TRANSLATION_RUBRIC` is non-empty, mentions tool-call, mentions ID preservation, does NOT bake source/target language into the prompt — cache-stability invariant); tool/schema lockstep (Anthropic `required` matches Gemini `required`, phase item `required` matches); payload builder (every translatable string surfaces, null descriptions render as empty strings rather than fabricating "null" literal); parser (valid → typed object, empty title → throws, non-object input → throws, non-array phases → throws, missing slot id → throws — drift safety); projector (re-keys by source ID against a position-scrambled model output, drops phase IDs fall through to source text, dropped slot IDs fall through to source text). The pure module has no DB, no provider — runs in milliseconds and locks the contract independent of the route's mocks.
  - **Cost gate (P10V "cost gate" — ≤ P0 budget):** structurally guaranteed by the runner architecture. Per-attempt usage is accumulated in a `usageMeter: Record<string, ModelUsage>` keyed by model id (so Sonnet and Haiku stay distinct even though both are Anthropic) and `computeCost(meter)` returns `{ usd, byModel }` using the shared `path-generator-cost.ts` rate card. The runner writes `costUsd`, `tokensIn`, `tokensOut`, `cacheReadTokens`, `cacheWriteTokens` to the `PathTranslation` row atomically with the `status='ready'` transition — a translation can't exist in `ready` state without its cost row, and the daily-budget guard's `SUM(costUsd)` aggregation in `isDailyBudgetExceeded()` reads from the same column. With Gemini 2.5 Flash @ $0.30/M input + $2.50/M output and the cached rubric block, a typical path's structural overlay (≈ 1.8–4.8K input tokens + ≈ 2K output tokens) prices at ≈ $0.001 — two orders of magnitude under the P0 §7.4 $0.10 target ceiling. The route's `incrementUsage` site post-success means failed translations don't burn quota either, so the per-user FREE worst-case (5 × $0.10 = $0.50 lifetime) and PRO worst-case (50 × $0.10 = $5/month) both clear the [`feedback_freemium_unit_economics`](../.claude/projects/-Users-toprakdemirel-Entwicklung-Quizzard/memory/feedback_freemium_unit_economics.md) ceilings comfortably.
  - **Code-quality gates:** `tsc --noEmit` exit 0 across `apps/web`; eslint exit 0 on every new file (the 4 new modules under `src/lib/translation/` + the 2 new test files) plus the modified `app/api/community/paths/[shareId]/route.ts` and `app/(dashboard)/learn/community/[shareId]/page.tsx`; full vitest suite **393 pass + 1 skip across 24 files** (was 356 + 1 skip / 22 files at P9V — net +37 tests across +2 new test files from the translation route integration suite + the prompt pure-module suite).

  Remaining open items rolled forward:
  - **Manual readable-translation walkthrough:** deferred to the deploy-time runbook — same shape as P4V/P5V/P6V/P7V/P8V/P9V's manual leg. Runbook is: (a) seed an approved DE path with non-trivial title + description + phase/slot titles; (b) sign in as a PRO user; (c) navigate to `/learn/community/<shareId>`; (d) click the `EN` chip; (e) assert the title + description + phase/slot titles swap to readable English (not Google-Translate-tier word-salad); (f) reload the page and assert the second visit is instant (cache hit, no spinner on the EN chip); (g) check `PathTranslation` row in SQL — `status='ready'`, `costUsd > 0` and ≤ $0.10, `tokensIn + tokensOut` populated, `provider='gemini-2.5-flash'`; (h) flip to FR / IT / TR — each is a fresh cache miss with the same readable-output expectation. The dev seed's library fixtures don't yet plant phase / slot content, so the readable-output check happens at deploy-time against a path that went through the full publish flow (or against a seed-extended fixture, future work).
  - **Popularity-trigger handoff:** the P0 §4.6 post-clone popularity-gate fan-out is still deferred to P11 (per the P9 status note above). P10 ships the canonical translation runner + cache surface; P11 will wire `setImmediate`-style popular-language fan-out on top of it without modifying the runner. The fan-out reuses the same `runTranslation` entry point — it'll iterate `POPULAR_LANGUAGES.filter(lang => lang !== source.language)`, calling the runner sequentially with backoff.
  - **Hallmark stamp on the LanguagePicker:** component-scope critique stamped inline (`P5 H4 E5 S4 R5 V4`). Macrostructure variety: the picker is a single state-machine component, not a macrostructure shift — the macrostructure stamp on the parent page surface (P8) is unchanged. The chip rail intentionally departs from the templated `<select>` dropdown that every SaaS library ships by default — the segmented chip pattern with `aria-pressed` + an inline `home` icon on the source chip (so the user can find their way back without parsing language codes) is the visible craft signal. Mobile responsiveness: the rail uses `flex-wrap: wrap` with `gap: 6px` so it collapses cleanly at 320px, and `letter-spacing` on the chip labels stays inside the editorial range. Light-mode audit: every text colour token (`var(--on-surface)`, `var(--on-surface-variant)`, `var(--on-primary)`, `var(--primary)`, `var(--error)`) respects the `feedback_light_mode_no_light_text` rule — no white-on-white surfaces under `[data-theme='light']`.

P11 may begin.

---

### P11 — Popular-language pre-translation (background, popularity-gated)
**Build:**
- Config: `POPULAR_LANGUAGES` env var (initial set from P0), `POPULARITY_THRESHOLD` env var (default 10 — see P0 spec §3 / §6).
- **Trigger (revised per P0 spec — popularity-gated, not eager):** the pre-translation fan-out fires under exactly three conditions: (a) on `approve` for **seeded** paths only (cache-warm the free-funnel anchor); (b) on **clone** when the post-increment `downloadCount` first crosses `POPULARITY_THRESHOLD` (atomic guard via `popularityTriggeredAt`); (c) on **admin manual trigger** via `POST /api/admin/paths/[shareId]/pretranslate` (`AdminAction: 'shared_path.pretranslate_force'`) — also covers one-off backfill of pre-rule rows. Fire-and-forget, sequential per language with backoff. Skip languages where a `PathTranslation` row already exists.
- The admin manual-trigger endpoint also serves the `POPULAR_LANGUAGES` set-change backfill use case.

**P11V — Verification gate:**
- **Seeded path** approved → polling for `PathTranslation` rows shows one per popular language within N seconds.
- **Community path** approved → `PathTranslation` rows stay zero. Then 10th clone arrives → fan-out fires within N seconds. 11th clone is a no-op (idempotency via `popularityTriggeredAt`).
- Viewing a pre-translated path in any popular lang hits cache, zero AI call (verified via mock provider counter).
- **Cost gate:** bulk pre-translation cost per popular/seeded path measured against P0 budget ≤ $0.50; total expected monthly pre-translation spend projected against `(seeded_count + community_popular_count_per_month)` — must clear the revised P0 ceiling of **$100/month**.
- Failure handling: if a per-language pre-translation fails, it's retried on the next view-time fallback to on-demand, not silently dropped.
- Concurrent clones at the threshold boundary → exactly one fan-out (race-tested).

---

### P12 — Curated seed library + free-tier switchover (the big one)
**Build:**
- Author and publish N≥20 curated seed paths via the admin dashboard. Seeded paths bypass moderation (marked `seeded=true` or published by an admin role — decision in P0).
- **Free-tier change:** `checkUsageLimit('ai_study_plan')` returns 0 for FREE tier (vs current 3). Implement behind a feature flag `FREE_TIER_AI_PATHS_DISABLED=true` so it can be reverted without redeploy.
- **Free-tier UX update:** when a free user hits the "create path" CTA, they land at the community library with messaging explaining that paths come from the library (and AI generation is part of Pro). Pro users see the unchanged flow.
- **Telemetry:** track free-user library-clone count, free-user retention (D1/D7), and AI COGS per free user before and after the flag flip.

**P12V — Verification gate:**
- E2E: brand-new free-user signup → onboarding → lands at library → clones a seed path → uses it through to first activity completion.
- Regression: Pro user can still generate AI paths (unchanged flow).
- **Activation measurement window:** 1 week with the flag on. AC for go/no-go:
  - Free-user activation rate (define activation in P0) does not drop more than X% vs baseline.
  - Free-user-week-1 retention does not drop more than Y%.
  - Free-user AI COGS drops to near-zero except translation tail.
- **Rollback plan rehearsed:** flip flag off, verify free users get AI generation back within 1 minute.
- Light-mode + dark-mode audit on the new free-tier "create path" landing UX.

---

### P13 — Reserved (Layer 4: post-publish reports / trust scoring)
**Build:** Deferred. Re-plan when P1–P12 are stable in production.
**Sketch:** `Report` model linked to SharedPath; user-facing "report this path" affordance; aggregation triggers re-moderation; trust score on `User` auto-flags first N publications.
**P13V — Verification gate:** TBD when designed.

---

## 6. Final acceptance gate (system-level, after P12V)

Trace each AC back to the original constraint that motivated this work:

- **AC-Economics:** measured per-free-user AI COGS ≤ 0.3 CHF target over a 7-day window post-switchover. Traces back to freemium unit economics.
- **AC-Front-door:** a brand-new free user gets onto a real path within 60 seconds of signup, no AI generation. Traces back to paths-led growth.
- **AC-Moderation:** zero public paths have ever bypassed moderation in audit logs. Verified by SQL: every `SharedPath.moderationStatus='approved'` has a corresponding `ModerationAudit` chain.
- **AC-Translation:** viewing a published path in any popular language never triggers an AI call (measured over a 24h window).
- **AC-Auditability:** every admin moderation action appears in `AdminAuditLog` with admin ID + target. No orphan tickets.
- **AC-a11y:** every new surface passes the light-mode-no-light-text rule + keyboard nav baseline.

---

## 7. Risks & mitigations

| Risk | Mitigation |
|---|---|
| L2/L3 cost overrun under load | Per-author submission rate limit; cost guard daily cap; cache-hit ratio monitored |
| Translation cost overrun | Per-language daily budget guard (P10); rate limit per user/IP; popular set kept conservatively small (start ≤ 6 langs) |
| Seed library quality too thin → free users churn | Manually curate; commit to N≥20 quality paths before flipping the FREE flag; activation/retention gates in P12V |
| Free-user activation regression | Feature-flagged switchover (P12); 1-week measurement; rehearsed rollback |
| SharedNotebook bypasses moderation today | Out of scope — but **flag for a separate plan** because the same risk applies and the moderation infra here is reusable |
| Coolify env vars missed | Pre-deploy checklist: every new env var (provider keys, popular-language set, feature flags) listed and confirmed in Coolify container env before merging |
| Moderation false negatives (bad content gets through) | L1+L2+L3 layered defense; P13 reports flow is the recovery; admin manual audit log is the forensic trail |
| Moderation false positives (good content rejected) | All rejections come with reason codes and an appeal path → ticket creation by author (not in v1, but the data model supports it) |

---

## 8. Out of scope (v1)

- Live-subscribe clone model (only fork-on-clone in v1).
- Creator attribution / monetization / "verified author" badges.
- Translation of user-private (unpublished) paths.
- Layer 4 (P13) — reports/trust scoring.
- Retrofitting moderation onto `SharedNotebook` — same problem, separate plan.
- Public/SEO-visible community surface (auth required for v1; SEO is v2).
- **Comments** on public paths — deferred to v2. Ratings (1–5 stars) **are in v1** per P0 spec §2.4 / §3.10; comments are held back because they'd double the moderation surface and we want rating-only data first to know if comments are needed. Reuses `NotebookComment` infra when revisited.

---

## 9. Open questions to resolve in P0

- Exact subject taxonomy for filters — reuse existing or extend?
- Public/unauth library vs auth-required — see "Out of scope".
- Seed-content rights — admin-published paths' visibility model (is `SharedPath.publishedById = adminId` enough, or do we need a `seeded` flag for analytics?).
- Numerical cost budgets per audit type and translation type — these become P4V/P5V/P10V/P11V gate thresholds.
- Popular-language set — proposal: de, en, fr, es, it, tr.
- Activation/retention regression tolerances for the AC in P12V.
