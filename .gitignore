node_modules/
**/node_modules/
.env
.env.local
.DS_Store
.next/
**/.next/
dist/
*.tsbuildinfo

# user uploads (local-only, never committed)
apps/web/uploads/
apps/web/public/uploads/

# claude worktrees
.claude/worktrees/

# local-only planning docs (working notes, not product)
plans/

# local-only skill installs (npx skills add nutlope/hallmark — lives in
# ~/.claude/skills via symlink; the in-repo copy is dev tooling, not product)
.agents/
.claude/skills/
skills-lock.json

# brand promo videos + render workspace (local-only, not product)
brand_video/

# graphify dated backup snapshots (auto-generated each rebuild, up to ~75MB each;
# exceed GitHub's 50MB limit). The canonical graph.json + GRAPH_REPORT.md at the
# graphify-out/ root stay tracked — only the timestamped backup dirs are ignored.
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
