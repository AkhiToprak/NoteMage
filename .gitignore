node_modules/
**/node_modules/
.env
.env.local
.DS_Store
.next/
**/.next/
dist/
*.tsbuildinfo

# user uploads (local-only, never committed)
apps/web/uploads/
apps/web/public/uploads/

# claude worktrees
.claude/worktrees/

# local-only planning docs (working notes, not product)
plans/

# local-only skill installs (npx skills add nutlope/hallmark — lives in
# ~/.claude/skills via symlink; the in-repo copy is dev tooling, not product)
.agents/
.claude/skills/
skills-lock.json